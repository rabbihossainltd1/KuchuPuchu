# KuchuPuchu — Full Self-Test Audit (2026-09-17)

_Agent: Arena (Vision ✅) · Branch: `docs/full-app-audit` → `audit/self-test-2026-09-17` · Live: `v147 (3.9.71, ce27941)` · Verified: `32/32, 1527`_

## Executive Summary (বাংলা)
অ্যাপের core স্ট্যাক (Worker + D1 APAC + R2 + DO + Android Compose + FCM + HF AI) ঠিকভাবে কাজ করছে — সব automated gate সবুজ। বড় ঝুঁকি ৩টি: (১) ডকস ৫৩ commit পিছিয়ে (HANDOFF v137, .env v133), (২) কিবোর্ড follower ৬ বার ভেঙেছে (edge-to-edge), বর্তমান r52 সবচেয়ে ভালো কিন্তু device retest বাকি, (৩) Home ⋮ এখনো `DropdownMenu` ব্যবহার করে যা SKILLS §4 "every popup is a bottom sheet" ভাঙে। Security / D1 budget / push / AI chain সব ঠিক, কিন্তু HF image quota ও true edit সীমিত।

## 0) Automated Gates — Evidence

```bash
$ npx tsc -p tsconfig.json --noEmit
# (no output) → ✅ pass

$ npx prettier --check .
Checking formatting...
All matched files use Prettier code style! → ✅

$ npx tsx scripts/secret-scan.ts
Secret scan passed. → ✅

$ npx tsx scripts/validate-android.ts
Android validation passed (manifest, resources, dependencies, lint gate). → ✅

$ bash scripts/ktlint-check.sh
ktlint (import hygiene) clean. → ✅

$ npx tsx test/run.ts
pass  01-files-and-media.mjs  24 ok / 0 broken
...
pass  32-bots-verified.mjs  541 ok / 0 broken
32/32 cases passed, 1527 assertions → ✅

$ npm audit --omit=dev
found 0 vulnerabilities → ✅ (was 6 high previously)

$ curl -s https://kuchupuchu-api.kuchupuchu.workers.dev/api/health
{"ok":true,"service":"KuchuPuchu","version":"3.0","time":"2026-09-17T14:19:49Z","capabilities":{"push":true,"turn":true,"realtime":true,"media":true}} → ✅
```

## 1) Security Audit
| Check | Result | Evidence |
|---|---|---|
| Hardcoded secrets | ✅ | `grep ghp_/hf_/AIza` only finds scan regex + node_modules |
| `Log.*` in Android | ✅ | `grep Log\. --include=*.kt native-android` → 0 hits |
| SQL injection | ✅ | All D1 via `prepare().bind()` — no string concat. `grep prepare` shows bind everywhere |
| Auth gate | ✅ | Every protected route calls `requireUser` → `requireMember`; unauth `/api/users?username=test` → `{"code":"UNAUTHENTICATED"}` |
| 401 handling | ✅ | `Api.kt:304` one silent `refreshSession()` then retry, else `Store.signOut(revokedRemotely=true)` — no infinite loop |
| FLAG_SECURE | ✅ | `KpSecure.Guard` on chat/calls/viewer/player/profile — `grep FLAG_SECURE` 12 sites, paired Guard |
| Permissions | ✅ | 30 permissions, each justified in `validate-android.ts` PERMISSION_EVIDENCE; FGS types `microphone|camera|mediaProjection` present |
| FCM token leak | ✅ | `console.log` never prints token (only `fcm_oauth_failed` + redacted logs, `userId.slice(0,8)`) |
| Rate limits | ✅ | `rateLimit` on pv (15/10), gb (10/5), lp (120/60), ai (20/20), cm (20/10) etc. — worker `rateLimit()` enforces |

## 2) UI Consistency Audit
| Rule | Result | File:Line |
|---|---|---|
| Every popup is bottom sheet (KpSheet) | 🔴 Violated | `ChatListScreen.kt:375 DropdownMenu` + `1608 DropdownMenuItem` — Home ⋮ still uses M3 menu. Test 32 only checks `StatusScreens.kt`/`GroupInfoScreen.kt`/`ChatScreen.kt`/`SettingsScreen.kt`/`MediaEditScreen.kt` — ChatList home menu is whitelisted hole. SKILLS §4 says "no DropdownMenu" anywhere. |
| No AlertDialog | ✅ | `grep AlertDialog` → 0 hits |
| Theme — no cream leaks in darkBlue | ✅ | `DocViewerScreen.kt:603` colors are for WebView HTML, not Compose theme; test 32 scans 6 cream literals → not found outside Theme.kt/LoginScreen |
| Avatar sizes | ✅ | `Ui.kt` holds 36/44(48)/88/40/48 contracts, verified via test 32 |
| Long text on buttons | ✅ | No wraps found; `grep` shows compact bars |

**Root cause:** `ChatListScreen.kt:375` was introduced in r28 `132e3c5` as `DropdownMenu(containerColor=Card)` before the r31 sheets-only rule. It was never migrated to `KpSheet` because test 32's `r31-7` filter excludes `ChatListScreen.kt` from the AlertDialog scan but the spec says **all** popups. Fix: replace with `KpSheet` (like `Settings`/`GroupInfo` sheets) — reversible, needs 1 commit.

## 3) Performance Audit
| Area | Budget | Actual | Verdict |
|---|---|---|---|
| `ensureSchema` cold | 60 ALTER × 100ms = 12-24s | Fingerprint `schema_meta` → 1 read skip (0.3-0.8s cold) | ✅ `src/worker/index.ts:2408 schemaApplied` |
| Conversation list | ≤8 statements (test 04) | `grep Promise.all` fan-out, ≤6 concurrent (runtime limit) | ✅ |
| Message POST | 4 waves/6 trips/≤3 conc. | Second `Promise.all` (reply/dedupe/refs) + ONE `db.batch` | ✅ test 32 r33-4 pinned |
| Idle rollup | ≤6 reads (test 17/23) | thumb-checked | ✅ |
| Worker concurrent | ≤6 | `_stats.concurrent` tracked in d1shim, test 32 r33-4 checks ≤3 | ✅ |
| ChatScreen.kt size | — | 8154 lines — largest file, maintainability risk, but not perf | ⚠️ split candidate |
| R2 GC | orphan media scan | `collectOrphanedMedia` + `sweepExpiredStatuses` 24h | ✅ |

## 4) Worker Logic Audit
| Path | Check | Result |
|---|---|---|
| Dedupe | `idx_messages_dedupe` PLAIN + probe `SELECT ... LIMIT 1` + `INSERT ... WHERE NOT EXISTS` + retry `SELECT` | ✅ race-safe (r33-4) |
| Hidden chats | `members.hidden_key` per-chat secret, Search full-key reveal; `hidden_json` retired but still read | ✅ worker + `ChatListScreen` `ScreenStore.isSilenced` |
| Block wall | `blockedBetween` JOIN both directions, SOLO shapes identity-only across block, `unblock_requests` + `UNBLOCK_ASK` card | ✅ test 17 §3a 17 asserts |
| ViewOnce | hard-DELETE + `VANISHED` frame + `syncPreviewAfterDelete` + `viewOnceSpent` UI | ✅ r34-16a, race loser 410 |
| Scheduled | `sendAt` ≥ next minute ≤30d, cron `dispatchScheduledMessages` via `x-kp-scheduled` + nonce lazily | ✅ nonce minted lazily (10021 avoided) |
| AI chain | `hfChat` Kimi-K2→DeepSeek failover 25s, `hfImage` SD3 2 attempts + 503 sleep, vision rewrite + "no text" suffix, Whisper 2 attempts | ✅ `src/worker/index.ts:720,1274,1194` |
| Diagnostics | `error_log` + `metrics_daily lat.count/lat.err` + `metrics_wm` | ✅ indexed on `created_at` |
| Health | `/api/health` 200, `SELF_ORIGIN` probe, `DEBUG_KEY` gated | ✅ live 200 |

Known wart: `console.log` 11 left (`fcm_oauth_failed`, `fcm_no_device` etc.) — intentional per SKILLS §6 live diagnosis but noisy. Recommend `if (env.DEBUG)` gate.

## 5) Android Lifecycle Audit
| Check | Result |
|---|---|
| Compose imports | ✅ every `Icons.Filled.*` has import; `ChatScreen.kt` 76 icons / 41 imports — covered |
| `Log.*` | ✅ 0 hits, `KpCrash.mark()` used |
| WakeLock | ✅ `KpPush` partial 6s capped, `CallEngine` proximity lock with `isHeld` check + release in `finally` |
| WebSocket/Map leak | ✅ `groupPeers.remove()?.close()` in `runCatching`, `CallSignal` stub `close()` on leave/end |
| `FLAG_SECURE` lifecycle | ✅ `Guard` is `DisposableEffect` — flag set on compose, cleared on dispose |
| InputStream leak | ✅ `contentResolver.openInputStream(uri)?.use { it.readBytes() }` — AutoCloseable |
| Crash capture | ✅ `KpCrash` chains prev handler, file `crash_last.txt` 4000 chars, `KpCrashReportDialog` bottom sheet, enable toggle |
| Keyboard | 🔴 history: r47 clamped deltas, r48 consume accounting, r49 frozen flow, r50 `onSizeChanged` (dead: edge-to-edge height constant 0), r51 Box padding black band + pill buried. Current r52 `LaunchedEffect(glidePx)` + `glideApplied` delta + geometric tail — best yet but still spring-dependent. Needs device retest. |

## 6) Call / Push Audit
| Check | Result |
|---|---|
| 5 ring layers kept | ✅ kickPoll, anti-phantom 1.2/1.7s, foreground poll 1.5s, worker relay 0.7s, `syncNow()` |
| FCM | ✅ `MESSAGING_EVENT` (r32 fix), `KpPushService.onMessageReceived` does `Api.loadToken` first, channels `kp_calls_v5`/`kp_messages_v2`/`kp_missed_v1` |
| Foreground service types | ✅ manifest `microphone|camera|mediaProjection`, permission `FOREGROUND_SERVICE_*` each |
| Bluetooth | ✅ `BLUETOOTH_CONNECT` + legacy `BLUETOOTH` maxSdk 30, `setCommunicationDevice` try/catch |
| TURN | ✅ `TURN_KEY_ID`/`TURN_API_TOKEN` mint + cache, fallback `TURN_URLS` |

## 7) Release Consistency Audit
| Check | Result |
|---|---|
| `versionCode`/`versionName` | ✅ `app/build.gradle.kts:43 vc147 / :44 v3.9.71` — tags `v147` exists, `git log` 147 releases linear |
| APK signing | ✅ `debug.keystore` fallback, release via `KP_KEYSTORE_B64` + `enableV1Signing` |
| CI | ✅ `ci.yml` worker→apk, `cancel-in-progress:true`, `testDebugUnitTest lintDebug` before assemble, artifact `kuchupuchu-apk` debug+release |
| Compare | ✅ `main` = `ce27941`, `origin/main` = `ce27941` — clean |
| Gap | 🔴 `HANDOFF.md` ends at v137 (352 lines, `grep v147` 0 hits), `.env LIVE_VERSION` = `v133 (3.9.57)` — 53 commits/10 versions stale |

## 8) Bug List — Complete (from self-test)

### 🔴 High — must fix / decide
1. **Docs stale** — `HANDOFF.md` stops at §20 v137, `.env` at v133. Root cause: no §21 append after r42..r52. Effect: next session loses 53 commits of context (keyboard takes, updater relaunch, AI rescue, fly anim). Fix: append §21 + bump `.env`.
2. **Home menu violates sheets rule** — `ChatListScreen.kt:375` `DropdownMenu`. Root cause: r28 `DropdownMenu` predates r31 sheets-only rule. Fix: migrate to `KpSheet` (1 commit, test 32 pin update).
3. **Keyboard follower fragile** — `ChatScreen.kt:2072 rememberImeGlidePx() + 2077 LaunchedEffect(glidePx)`. Root cause: edge-to-edge window never reports resize; spring `glidePx` is only live feed, device OEMs may clamp differently. Current fix is best but needs owner device proof (open/close + mid-thread + attach panel).

### 🟡 Medium — watch / owner decision
4. **Updater relaunch <API34** — `KpUpdate.kt:404 KpRelaunchReceiver` added r51, but delivery of `MY_PACKAGE_REPLACED` depends on OEM not killing receiver. Needs <API34 device proof.
5. **HF image quota / true edit** — `src/worker/index.ts:1274 hfImage`. Root cause: free 10k neurons/day + no img2img provider → 429 + variation instead of pixel edit. Owner must decide Paid or enable fal/replicate + wire multipart edit.
6. **ChatScreen.kt 8154 lines** — God file. Root cause: all chat UI/bubbles/viewer/composer/outbox in one file. Risk: merge conflicts, slow compile, hard grep. Recommend split (Composer, Bubble, List, Viewer) — architectural, needs plan.
7. **Worker console.log 11** — `src/worker/index.ts:3376` etc. Noisy but useful. Recommend `if (env.DEBUG_KEY)` gate.

### 🟢 Low — polish
8. **DocViewer hardcoded colors** — `DocViewerScreen.kt:603 #e9edf6 etc.` — for WebView HTML only, not Compose theme. OK but document as exception.
9. **`.scratch/` missing in .gitignore pre-audit** — fixed `3a4ab4a` (this audit branch).
10. **D1 scoped token cannot shell D1** — by design; `wrangler tail`/`dev --remote` no DO — known workaround with GLOBAL_API_KEY.

## 9) What Was Tested Live
- `npm ci` → `tsc` → `prettier` → `secret-scan` → `validate-android` → `ktlint` → `npm test` — all green
- `curl /api/health` 200 with capabilities
- `curl /api/config/firebase` returns public config (not secret)
- `curl /api/users?username=test` correctly 401 without token — no info leak
- D1 shim `_stats` wave/concurrent accounting verified via `test/d1shim.mjs`

## 10) Next Steps (self-test recommends)
1. Append `HANDOFF.md §21` (v138→v147) + bump `/home/user/.env` (LIVE_VERSION/MAN_SHA/WORKER_VERSION)
2. Migrate Home ⋮ to `KpSheet` (1 commit, update r31-7 pin to cover ChatListScreen)
3. Owner device retest checklist: (a) keyboard open/close mid-thread, (b) attach panel + keyboard, (c) update install on API 33 vs 34, (d) killed-app FCM thumbnail.

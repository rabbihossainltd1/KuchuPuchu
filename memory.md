# Project Memory

## Round 58 User Directives & Status
- Directive 1: See more expand, message body collapse with animation:
  - User feedback: "not fixed+ tumi expand animation taw remove korcho but why? tumi ager motoi rakho see more a click korle expand hobe massage body te click korle collapse hobe animation er sathe hobe shob."
  - Fix:
    1. Re-enabled message body collapse on expanded messages: in `Box.combinedClickable.onClick`, when `msgExpanded` is true, clicking anywhere on the message body collapses it back (`msgExpanded = false`).
    2. Maintained smooth spring collapse/expand animation via `.animateContentSize(animationSpec = spring(dampingRatio = 0.85f, stiffness = 400f))`.
    3. Clicking "See more" expands the message; clicking the message body or "See less" collapses it.
- Directive 2: Message send animation starting points & physics:
  - User feedback: "ar all massage items sending animation er start point thik nai . massage text er start point hobe massage composer pill er upor theke niche theke na. ar baki items gula jemon voice massage ta voice voice button theke choto theke original voice massage er size a joye nijer position a chole jabe. ar photo video documents attach panel theke jump korbe."
  - Fix:
    1. Text message flight start point: In `SendFlight.kt`, updated `startY = pill.top - s.height` so the bubble lifts off directly from the top of the composer pill, never from below the screen or below the pill.
    2. Voice message launch: Added `fxVoiceLaunch` in `ChatFx.kt` and anchored to `FlightAnchors.micBounds`. Starts at the mic button, scales up from small (0.28f) to full original size (1.0f), and glides smoothly into its seat position.
    3. Media & document jump: Added `fxAttachJump` in `ChatFx.kt` and anchored to `FlightAnchors.attachBounds`. Photos, videos, and documents execute an upward parabolic jump arc from the attach panel / paperclip button to their bubble seat.
- Directive 3: Bumped version to `v190` (`versionCode = 190`, `versionName = "3.9.113"`).

## Round 60 User Directives & Status
- Directive 1: Diagonal slide send/receive flight animation:
  - Sent messages: animate smoothly into chat diagonally from the bottom-right (+44dp X, +18dp Y to 0,0) with soft spring ease and alpha ramp.
  - Received messages: animate smoothly into chat diagonally from the bottom-left (-44dp X, +18dp Y to 0,0).
  - Chat history scrolling and paging remain quiet with no sliding animation.
- Directive 2: View-once media bubble refinements:
  - Center the view-once icon perfectly within the bubble border (`CenteredOnceIcon(40.dp)` on `Alignment.Center`).
  - View-once card size reduced (`widthIn(max = 138.dp)`, `heightIn(max = 175.dp)`, min fallback `widthIn(min = 108.dp).height(138.dp)`).
  - Sending echo retains media aspect ratio while keeping verbatim `metaWith` pattern and test assertions intact.
- Directive 3: Received short message bubble sizing:
  - Compact bubble width `requiredWidthIn(min = if (!mine) 78.dp else 79.dp)` applied to both received and sent short messages.
- Directive 4: Last seen text & presence:
  - 3-letter abbreviations (`yes`, `sun`, `mon`, etc.) implemented across `ChatScreen.kt`, `Theme.kt`, `Ui.kt`, `SettingsScreen.kt`.
  - `ONLINE_WINDOW_MS` reduced to 35s in `src/shared/constants.ts`.
  - Background push media fetches and `/api/calls/active` excluded from updating `last_active_at` in `src/worker/index.ts`.
  - `conv.value` assignment and metadata refresh on message arrival/polling enhanced in `ChatScreen.kt`.
- Directive 5: Version bumped to `v192` (`versionCode = 192`, `versionName = "3.9.115"`), released on GitHub, and Cloudflare Worker deployed.

## Round 61 User Directives & Status
- Directive 1: Corner flight animation coming further down from bottom corner:
  - Sent messages: animate smoothly into chat diagonally from the bottom-right (+68dp X, +84dp Y to 0,0) with soft ease and alpha ramp.
  - Received messages: animate smoothly into chat diagonally from the bottom-left (-68dp X, +84dp Y to 0,0).
  - All items (text, voice, photo, video, document, album, view-once) animated consistently.
  - Removed dependency on `pill == null` or `seat == null` aborts in `SendFlight.kt` so animation reliably triggers every time.
- Directive 2: View-once media bubble refinements:
  - Enlarged center view-once badge (`size(62.dp)`, `CenteredOnceIcon(48.dp)`) with smooth breathing pulse scale animation (0.92f <-> 1.08f) before viewed.
  - Removed top-left capsule pill (`Icons.Filled.Refresh` + `1`).
  - Removed dim background box from timestamp, rendered clean single-line timestamp in bottom right corner with smaller font (10.sp).
  - Fixed ratio jumping on send/sent: sender's `paintSent` inherits `mediaW`/`mediaH` and populates `ImageRatios` from donor pending echo; `photoUrlOf` prefers `kpLocalUrl`.
  - Allowed sender to view their own sent view-once media until the opponent views and vanishes it (`canOpen = !pendingEcho`, `onShown` and `kpOnce` gated so sender views don't spend the single opening).
- Directive 3: Received short message bubble sizing:
  - Reduced minimum width for received short text messages to compact `52.dp` (`requiredWidthIn(min = if (!mine) 52.dp else 70.dp)`), making received short messages like "hi" or "ok" truly compact.
- Directive 4: Version bumped to `v193` (`versionCode = 193`, `versionName = "3.9.116"`).

## Round 62 User Directives & Status
- Directive 1: Voice message flight animation & layout fix:
  - User feedback: "1. fixed almost just voice massage er animation er somoy original body na hoye fake animation hocche left side a extra space dekha jacche original rakho."
  - Root Cause:
    1. In `FileBubble`, `VoiceWave` was passed `grow = fxGrow` (`fxGrow = fxFresh`), which caused waveform bars to reveal one by one over time (`reveal = (growAt - 40f) / 22f`), rendering an empty canvas area initially instead of showing the complete real waveform.
    2. In `MessageRow`, `Box` had `.animateContentSize(...)` which animated the bubble's width during entry, causing it to stretch and move its left boundary.
    3. In `MessageRow`, `Box` had `.fxSideSlide(...)` which ran simultaneously with the outer `Row.fxFlyIn(...)`, doubling the horizontal translation and pushing the bubble too far right while creating empty space on the left.
  - Fix:
    1. Set `grow = false` in `VoiceWave` so the complete, original waveform is rendered immediately with all bars visible from the very first frame.
    2. Restricted `animateContentSize` to `textLike && longBody` only, preventing any size animation or stretching on voice message bubbles.
    3. Excluded `voiceRow` from duplicate `fxSideSlide` so voice messages glide smoothly via `Row.fxFlyIn` without duplicate offset or left-side void.
- Directive 2: View-once icon styling & animation:
  - User feedback: "2. shob ok just tomar zoom in zoom out animation ta pochondo hoi i ar ei icons er dim background remove koro just icon ta animate korbe."
  - Fix:
    1. Removed zoom-in / zoom-out pulse scale (`pulseScale` 0.92f <-> 1.08f) from center view-once badge.
    2. Removed the 62dp circular dim backdrop (`.clip(CircleShape).background(Color(0x66000000))`) so the icon floats directly over the blurred media with no dim disc behind it.
    3. Implemented clean continuous rotation on the outer dotted ring of the view-once icon (`ringRotation` 0f -> 360f), while keeping the central bold digit "1" stationary and upright, ensuring only the icon itself animates cleanly without any breathing scale or dim background.
- Directive 3: Received short message bubble size confirmation:
  - User confirmed: "3. fixed". Maintained compact 52.dp minimum width for received short messages and 70.dp for sent.
- Directive 4: Bumped version to `v194` (`versionCode = 194`, `versionName = "3.9.117"`).

## Test Gates
- All 37/37 test cases passing (1611 assertions).
- ktlint clean.
- TypeScript / typecheck clean.
- Prettier clean.
- Secret scan passed.
- Android validation passed.

## Audit 2026-09-21 (Arena agent, read-only — no push/deploy)
- Scope: full self-audit at HEAD 8a9b631 (v194): security, UI (static), performance, bugs. Full list: `AUDIT-2026-09-21.md`; project map: `PROJECT_UNDERSTANDING.md`.
- Gates re-run locally, all green: 37/37 tests (1611 assertions), tsc, prettier, secret-scan, validate-android, ktlint. HEAD CI #35538569060 success.
- HIGH: (1) AI down in prod — HF credits exhausted 09-17 (error_log 2-4/day → 186-317/day, `hf-chat 402` + `gemini 503`); (2) logged-out takeover via self-attested SIM on no-live-device path (`index.ts:5388`, no 2nd factor); (3) view-once media fetchable without spend (`index.ts:9163`, honest-client only).
- MEDIUM: enumeration oracles; per-isolate rate limits; no per-user storage quota + unattached uploads never swept; debug.keystore committed (needs owner call); v194 on TWO commits (8a9b631+1e0e252, updater won't offer 2nd); latency self-probe 100% failing (lat.count=0); ~1s chat poll cost.
- LOW/INFO: CORS *, DEBUG_KEY in query, unused timingSafeEqualHex, public TURN fallback, no delete time-limit (confirm intentional), stale debug/errors refs, commented code in ChatScreen, 4 APK assets on v194, red-main CI pattern, index-keyed photo grid, no pinning, 16ms FX loops.
- UI verdict: Round 62 + 1e0e252 diffs reviewed line-by-line, no static glitch; real visual check impossible here (no Android build/emulator in sandbox) — needs owner device / CI APK.
- Live: /api/health 200, releases/latest v194, error_log 1050 rows (7d). Origin ahead by 1e0e252 (ChatScreen-only) — local tree intentionally kept at 8a9b631 for stable audit line numbers.

## Fix loop 2026-09-21/22 (Arena agent; order H3→M1→M2→M4→M5→M6→M7→E1..E8→release; CI green per item)
- H3 done, commit `7121277`, pushed to main (owner-overridden flow: push-to-main, PR skipped — see git-flow conflict note).
- M2 done, commit `2e64800`, pushed to main.
- M1 (enumeration hardening) done, commit `6ff3e2a`, pushed; deployed Version ID `d9088ece-91e3-4985-8975-5f769b6ec1a9`; live-verified (start-unknown → 404 NO_RECOVERY_TARGET; lookup exists:false + masked RECOVERY_LOOKUP audit row). Gates 40/40 (1643 assertions). test31 lock updated same-commit (wrong-Google denial 401→404).
- M4 phase 1 (debug-key migration, same bytes) done, commit `6e66c04`, pushed; no worker change → no deploy. gradle reads KP_DEBUG_KEYSTORE_B64 secret, falls back to committed file, prints `kp-debug-keystore source=… sha256=…`. Continuity hash (public): `1e595e947dd292dced9fc223fee469405be0894740cdaacde17e7c9a4d0c8a6b`. Phase 2 (delete file, AGP-default fallback) waits on owner confirming the secret. Rotation ruled out: CI signs with the committed key (de-facto prod key). Gates 41/41 (1646 assertions).
- M5 done at release: version bump 194/3.9.117 -> 195/3.9.118 is the fix (release discipline); tag v195 + GitHub Release with CI APKs; updater (KpUpdate.kt) picks it from releases/latest.
- M6 (latency self-probe 100% failing) done, commit `7cf30da`, pushed; deployed Version ID `0c68322e-6308-4353-86ea-9bc014889`. Root cause (index.ts:5071): the cron isolate's network subrequest to SELF_ORIGIN never returned ok (egress leg — same URL 200s for outside traffic incl. every UA tested). Fix: scheduled() passes an in-process fetcher (handle(), same isolate); URL/timeout/counters unchanged. Probe doc corrected (health runs no D1 — number is handler time, sub-ms → sum_ms 0). test27's fixed 4000-char scheduled() window bumped to 4600 (log line pushed to +4035). Gates: `npm run ci` exit 0, 41/41 (1646 assertions). Live-verified at 23:00Z tick 2026-09-20: lat.count=3 (first samples ever), lat.err=69 (23 failed hrs × 3 — arithmetic exact), lat.sum_ms=0.
- CI pattern: cancel-in-progress works (older mains superseded); e.g. M6 run 35542945952 completed success while 6e66c04/6ff3e2a/2e64800/7121277 runs cancelled.
- D1 REST reminder: var is $D1_DATABASE_ID with X-Auth-Email/X-Auth-Key (global key); the scoped CLOUDFLARE_API_TOKEN has no D1 scope (10000 auth error); $D/$R do not exist.
- M7 (aggressive polling) done, commit `674917f`, app-only (no worker change -> no deploy; ships with release APK). Audit premises re-checked: tree already socket-primary (chat r6/r14 net, adaptive call net, PollCadence inbox, worker 0.7s one-shot). Change: ChatScreen safety-net timer restarts on any same-conv frame + 8s->30s backoff after 2min idle (no frame + no in-flight send). Call/login cadences untouched (r24 lock + owner rules). test32 r14 lock updated same-commit. Gates: `npm run ci` exit 0, 41/41 (1646 assertions). NOTE: node_modules does not persist across turns (snapshot exclusion) — rerun `npm ci` first in a fresh turn.
- E1 (see-less exact-text collapse) done, commit `657d3cf`, app-only (no worker change -> no deploy). Root cause (ChatScreen.kt:6256-6274): triple tap-handler stack double-toggled exact-text taps into a no-op. Fix: keep only the row pointerInput (1dcd592 on-device-proven); row clickable + text pointerInput deleted. Past attempts reviewed: 74b3f8d (exact-toggle rule), d51b774 (full-width target), 1dcd592 (pointerInput fix for dead clickable), 78b5e93 (body-collapse restore). Animation kept (msgExpanded + spring animateContentSize untouched). test32 +1 E1 lock, test37's 2 clickable pins moved to pointerInput same-commit. Gates: `npm run ci` exit 0, 41/41 (1647 assertions).
- E2 (typing/voice indicator 4px down) done, commit `ad86115`, app-only (no worker change -> no deploy). TypingBubble + RecordingBubble rows: symmetric vertical 3dp -> top 7dp / bottom 3dp (layout-aware shift; offset() would overlap). test32 +1 E2 lock (both rows pinned, count==2). Gates: `npm run ci` exit 0, 41/41 (1648 assertions).
- E3 (sending/sent identical for all types) done, commit `5a42a60`, app-only (no worker change -> no deploy). Sweep: photo/video/voice/doc/album/view-once/text/sticker/link echoes already carry first-frame ratio+thumb (v166/r62 work). Gaps fixed: (1) sending tick was 12dp vs 13dp sent/delivered/seen (TickIcon, shared by ALL mine types) -> 13dp; (2) fallback wave bars seeded from server id -> redrawn on echo->sent swap for audio without waveform -> seed from shared clientId; (3) audio-as-media (forwarded mp3) had no duration (echo "0:00", sent showed size) -> VideoFacts.probeAudioMs + seconds in echo+row meta. test32: r31-27 pseudo pin updated + new E3 lock. Gates: `npm run ci` exit 0, 41/41 (1649 assertions).
- E4 (calls-only E2EE verify; owner picked "calls" over secret-chats/full — message E2EE deferred) done, commit `72c86ea`, app-only (no worker change -> no deploy). Media is DTLS-SRTP; gap was unverified server-relayed SDP. Build: stable DTLS identity (RtcCertificatePem, private prefs, warmed at init) set on both pc creation sites (1:1 newPc + group newGroupPc; relayConfig untouched — setConfiguration only); per-call order-independent safety codes from both SDP fingerprints; TOFU peer store + change warning + trust ceremony; lock row on voice+video + KpSheet (no AlertDialog); groups show nothing. Zero latency: local parse+sha256+prefs only (locked by !includes("Api.")). WebRTC API verified against the real stream-webrtc-android-1.1.3 AAR before writing. Gates: `npm run ci` exit 0, 41/41 (1650 assertions).
- E5 (repo clean) done, commit `a69ee3f`, no code change (no deploy). Deleted after reference+freshness verification: SPEED_AND_PERMISSIONS_PLAN.md (45KB completed Sep-5 plan, zero refs), send-animations-preview.html (44KB fulfilled preview of shipped send FX, zero refs), DEPLOY-GUIDE.md (stale realtime-steps framing; live deploy/verify/rollback lines already covered by .env comments). Kept: memory/README/ARCHITECTURE-v3 (worker refs)/PHONE_AUTH_PLAN (many refs)/FIREBASE-SETUP+docs/native-plan (README refs)/docs/cloudflare-token (accurate runbook)/audio-routing+image-cache (test contracts)/AUDIT-2026-09-21+PROJECT_UNDERSTANDING (active loop basis — post-release archival candidate)/scripts (CI-used or live tooling). Gates: `npm run ci` exit 0, 41/41 (1650 assertions).
- E6 (keyboard closes on call arrival) done, commit `199686c`, app-only (no worker change -> no deploy). Root cause: CallGate is an overlay above everything (KpApp.kt:295) while the chat composer below keeps IME focus, so the keyboard parked over the call UI. Fix: LaunchedEffect(call.id) in CallGate hides IME + force-clears focus on arrival (codebase recipe from ChatScreen closeKeyboard). test32 +1 E6 lock. Gates: `npm run ci` exit 0, 41/41 (1651 assertions).
- E7 (old-history scroll animation) done, commit `9d27700`, app-only (no worker change -> no deploy). New fxHistoryUnfurl (ChatFx.kt): soft one-shot fade+14dp-rise 260ms, unique vs live slide/fly/pop. loadOlder keys each fresh page (historyFxKeys); both list-item sites apply + consume on first compose (no replay); reduced-motion gated; no listState touch (r52 no-yank intact). CONFLICT NOTED: overrides r45-item-1/r50/r58 + ChatScreen:6074 "history stays quiet" by explicit owner ask, for loadOlder rows only; flicker-proof by construction. test32 +1 E7 lock. Gates: `npm run ci` exit 0, 41/41 (1652 assertions).
- E8 (all message items max 70% width) done, commit `df15e68`, app-only (no worker change -> no deploy). bubbleMax factor 0.82 -> 0.70, floor 280 -> 240 (cap 420 kept); OwnerCard cardMax 0.92 -> 0.70, floor 300 -> 240, cap 440 -> 420; albums clamp via albumW = minOf(albumWidth, width * 0.70); FileBubble inherits ceiling via widthIn inside bubble Box; CallLogBubble left intrinsic (timestamps never cap short). Locks: test32 E8 check (exactly three 0.70 factors, no 0.82/0.92, albumW clamp) + owner-card pin 92% -> 70%; test36 v166-new#4 follows new formula. Gates: `npm run ci` exit 0, 41/41 (1653 assertions).

## Round 2 2026-09-21 (owner feedback on v195 + 3 new items; E3f needs worker deploy)
- E3f (view-once 1 s fake ratio after send) done, commit `06581cc`, WORKER+app -> deployed Version ID `04a07c7f-62f8-46f0-b47a-5cd075cf7ec4`, live health 200. Root cause (index.ts:8374): server stripped meta.w/h on once-rows (`!viewOnce ? dims : {}`), so every server copy lacked mediaW/H and ViewOnceRow fell to the 108x138 placeholder until the blurred thumb decoded. Fix: store dims (album exclusion + H3 spend + spent-hiding untouched); client payloads carry w/h under the once flag (metaWith, clipMeta-preferred file payload, both scheduled paths measure their own box). CONFLICT NOTED: overrides r32-17 + v163 "once publishes no dimensions" by explicit owner ask (dims leak nothing past the blur); pins migrated same-commit (test17 x2, test32 x2 incl. app-bytes pin, test33 x1) + test32 E3f lock. Gates: 41/41 (1654).
- E4f+N1+N2 done, commit `dbcd39e`, app-only (ships with next release APK). E4f: E2eeCodeRow reads plain "End-to-end encrypted"; tap swaps code in 3 s (auto-hide), tap-while-visible opens verify sheet; warn path unchanged. N1: faint white ring removed from CallAction/CallCircle/StripAction (fill+shadow carry the circle); form-field borders (Login/Ui) kept — not transparent buttons. N2: message list Arrangement.spacedBy(3.dp, Alignment.Bottom). test32 +3 locks. Gates: 41/41 (1657). CI `35611548854` success covers both commits.
- N3a (sticker-emoji bubble removal) done, commit `9c990b6`, app-only. STICKER rows share the emoji-only path (noBubble): no min width, no shadow, transparent fill, wallpaper-ink ticks, both sides. test32 N3a lock; r32-8 + both v169 stamp pins migrated same-commit. Gates: 41/41 (1658).
- N3b (per-emoji internal animation) done, commit `8252cbc`, app-only. New EmojiAnim.kt: 10 Canvas-drawn faces (JOY/ROLL/GRIN/SMILE/HEART/CRY/WOW/ANGRY/FLAT/SLEEP), each part-animated (chomping mouths, squinting eyes, falling/sliding tears, beating hearts, darting pupils, rising Z's); face outline never moves, zero whole-glyph transforms (locked by !graphicsLayer). Routed for single/multi/sticker rows with phase-shifted siblings; non-curated stay static glyphs; reduced-motion frozen. CONFLICT NOTED: overrides r44 generic emoji entry/idle for message rows by explicit owner ask (AnimatedEmoji kept for registry/tests). Pins migrated: test37 v169-item-7 call, test32 r31-12/r32-8/r34-19/v169-PLAIN, test36 new#5 (maxLines/countLines 4->3 — emoji bodies never fold). Gates: 41/41 (1659). Follow-up compile fixes x3 (CI apk red x3, all green on `9e814fd`, run 35617800727): 79558c5 animateFloat import + DrawScope.size shadow (size->faceSize), eb9696f missing layout.size import (the true 1st error — my shadow diagnosis was wrong), 9e814fd toPx is a Dp member-extension (faceSize.toPx()).
- Release v196 (3.9.119) cut, tag `v196` + GitHub Release with CI APKs (same 4-asset layout as v194/v195). Ships Round 2: E3f worker already live (`04a07c7f`, no deploy at release), E4f/N1/N2/N3a/N3b app-only via these APKs. test36 version pin migrated same-commit (owner precedent).

## Round 3 2026-09-21 (owner verdicts on v196: E3/E4/N2 fixed, N1 not fixed, N3 faces rejected + N4/N5/N6 new; worker deploy at end)
- N1r (faint circle still visible) done, in commit `ebc32b6`, app-only. Root cause REVISED (CallScreens.kt:720/1425): the visible ring was never the 1dp border N1 removed — it was the translucent gradient disc (CallAction 0x42FFFFFF->0x1AFFFFFF, StripAction 0x3DFFFFFF->0x14FFFFFF) + 6/5dp shadow. Fix: inactive branch fully bare (no fill, no shadow via conditional .then), danger/active fills stay. test32 N1r lock (both fills absent + both conditional shadows present).
- N3r (N3 rework: Canvas faces rejected, real glyph must dance) done, in commit `ebc32b6`, WORKER+app -> deployed Version ID `177cf500-0609-4301-a7ff-a552710c62f7`, live health 200 + /fx answers 401-shape unauthenticated. EmojiAnim.kt rewritten: 14-pattern hash-assigned 3D palette (graphicsLayer rotationX/Y/Z + cameraDistance 8*density: coin/nod/swing/hop/wobble/thump/tiltspin/rocker/peek/float/lean/pulse3d/roll/heartbeat-side) covering ALL emojis; 3 s auto-stop (LaunchedEffect delay -> static Text swap, never frozen mid-pose, no perpetual recomposition); tap replays + POSTs /api/messages/:id/fx (echoes excluded by c_ prefix); worker fans one emoji_fx room frame (member-only, TEXT/STICKER-only, 3 s per-message damper + fx:uid rate bucket) and ChatScreen's frame switch feeds module-level emojiFxReplays consumed once per row; live arrivals auto-play (fxFresh), history rests until tapped; reduced-motion never plays. CONFLICT NOTED: inverts the N3b no-graphicsLayer lock by explicit owner order; test32 N3b lock replaced by N3r lock, test32 r31-12/r32-8/r34-19/v169 titles+pins + test37 v169-item-7 pins migrated, test09 +4 N3r endpoint checks (replay shape, single frame, damper, 403 stranger).
- N4 (fake double-tick while recipient offline) done, in commit `ebc32b6`, worker (deployed with N3r above). Root cause (index.ts pushMessageToMember): delivered_at stamped whenever FCM 200-accepted the push — FCM-accept proves nothing about the app receiving the row. Fix: mark only on a live socket frame (pokeUserConversation sent-count > 0); pushToUser awaited but unbound; reconnect/fetch backstops untouched. Test09 stub parametrized (pokeSent, default 0 = offline reality) + 2 N4 checks (live stamps at send, offline stays NULL); r32-28 comment migrated; no other file pins the old write.
- N5 (view-once galaxy must scurry) done, in commit `ebc32b6`, app-only. Starfield stars orbit small seamless loops (2-4 integer turns per 2400 ms twinkle cycle, amplitude from star radius) while twinkling; center mark static; no opened-gating needed (spent rows vanish via H3, ViewOnceRow always means unopened). test32 N5 lock.
- N6 (compact attach tiles + swipe-down dismiss) done, in commit `ebc32b6`, app-only. AttachTile: 32dp seat / 22dp glyph / label row gone (talkback keeps contentDescription); collapsed-handle swipe-down >70px dismisses (fullscreen still folds; bar/header drags untouched); sel non-empty -> KpConfirmSheet Deselect (= clear via onDismiss honoring r45-5 + close) / Not now (= stay); empty swipes close straight away. System-back instant close (r45-4) unchanged. test32 +2 N6 locks.
- CI round 3: `35627363476` red x2 (kotlinc: receiver-lambda property must be invoked pattern.move(this, ph, density); prettier --check on the r31-12 migration) -> fix commit `3dc7ae7` -> `35627832173` attempt-1 red on INFRA (NDK 27.0.12077973 zip corrupt, ZipException — reran failed jobs, no code change) -> attempt-2 success. Gates: 41/41 (1669 assertions: +10 over v196).
- Release v197 (3.9.120) cut, tag `v197` + GitHub Release with CI APKs (same 4-asset layout). Ships Round 3: N1r/N3r-app/N5/N6 app-only via these APKs; N4 + N3r-worker already live (`177cf500`, no deploy at release). test36 version pin migrated same-commit (owner precedent).
- Release v198 (3.9.121) cut, tag `v198` + GitHub Release with CI APKs. Ships Round 4: N1 ring-only inactive call buttons, N3 real-3D without scale + animate on send, N4 list double-tick source fix, N5 slow random galaxy, N6 breathing tiles (40dp/26dp + gaps), N7 always-visible bigger caption bar (34/36dp, no black bar). test36 version pin migrated same-commit.
- Release v199 (3.9.122) cut, tag `v199` + GitHub Release with CI APKs. Ships fixup: N3 3D extrusion for vetorer objects, N4 delivered guard with otherActiveAt, N6 rounded panel, N7 swipe anywhere + Card bar + positionChange import. test36 version pin migrated.
- Release v200 (3.9.123) cut, tag `v200` + GitHub Release. Reverts attach panel UI to original (32dp/22dp, box, SpaceEvenly) per owner, keeps send 34dp big + panel swipe-down Initial fix, removes chat-list ticks entirely per owner. test36 pin migrated.

## Round 63 (owner feedback on v210; items 1, 3, 4 done, item 2 pending architecture)
- r63-1 (panel emoji bigger real shape without zoom/clip, single emoji in chat 66f) done, commit `0694490`, app-only.
  - Root cause: Box was 28dp inside a 36dp cell with clip(6.dp) and scale(1.2f), causing glyph edges to clip. Text(24.sp) was larger than LottieAnimation(26.dp) so it shrank visibly when loading. rememberLottieComposition was invoked unconditionally on all 800+ grid items during composition, firing hundreds of HTTP requests and causing scrolling stutter. Single emoji in chat was 52f.
  - Fix: StickerSheet.kt cell height increased to 42.dp; Box size set to 34.dp with no unwanted scaling or clipping. Text(26.sp, lineHeight=34.sp) and LottieAnimation(fillMaxSize) share the exact 34.dp box so size is 100% stable before and after animation. Instant animation load from local APK assets for 51 bundled smileys (NotoBundled.isBundled), 300ms debounce for remote emojis, keeping fast scroll 100% lag-free. ChatScreen.kt single emoji enlarged to 66f; multiple emojis kept at 40f. test32 + test37 locks updated same-commit. Gates: 41/41 (1670 assertions).
- r63-3 (composer pill and voice recording bar background transparent) done, commit `acc4a98`, app-only.
  - Root cause: ChatScreen.kt Composer pill Column explicitly painted .background(Card) at line 4530. Previous 10+ sessions failed to remove it because test 32 (r18-5 and r19-theme) locked .background(Card) and strictly forbade .weight(1f).padding(...) without card background.
  - Fix: Overrode r18-5 / r19-theme locks to admit .background(Color.Transparent) on composer pill Column. Live voice recording strip already uses transparent background. test32 updated with r63-3 lock. Gates: 41/41 (1671 assertions).
- r63-4 (attach panel selection bar floating transparent with bigger controls) done, commit `d21d079`, app-only.
  - Root cause: AttachSheet.kt only showed the media selection bar when fullscreen was true (`if (fullscreen && sel.isNotEmpty())`), so in the half-panel the bar was hidden. Controls were tiny (28dp seat, 14dp icons) and the bar did not float with a transparent background. Half-panel tapping a media item forced setFullscreen(true), abruptly yanking the view.
  - Fix: AttachSheet.kt selection bar now appears on `if (sel.isNotEmpty())` for both half and full panel. Selection bar background set to Color.Transparent. Controls enlarged: Send button 44dp / icon 22dp / badge 18dp; Caption bar 40dp / text 15sp; View-once 38dp / icon 36dp; Edit 38dp / icon 20dp. MediaCell onToggle in half-panel no longer forces setFullscreen(true). test32 updated with r63-4 lock. Gates: 41/41 (1672 assertions).

## Round 63 Follow-up (owner feedback on v211: items 2 & 3 rework; v212 / 3.9.135)
- Item 1 (emoji panel): Confirmed fixed by owner.
- Item 2 (composer pill background & border):
  - Root cause: previous session changed Composer pill background to Transparent without an explicit border (pill shape vanished), and CoinWallpaper was only drawn inside the LazyColumn Box, leaving a flat solid background behind Composer.
  - Fix: Root Box in ChatScreen now hosts CoinWallpaper across the entire screen so coins wallpaper extends seamlessly behind composer, voice recording strip, and attach panel. Composer pill Column given explicit 1.dp border (`Line.copy(alpha = 0.55f)`) with transparent background, keeping the pill boundary clearly defined over the chat wallpaper.
- Item 3 (attach panel floating transparent selection bar, keyboard IME glide, and half-panel stability):
  - Root cause:
    1) AttachPanel Column had `.background(Cream)`, rendering a solid navy panel instead of transparent.
    2) When media was selected in half panel, `ChatScreen.kt` had `else if (!showAttach || (attachSel.isEmpty() && !attachFs))`, abruptly hiding Composer and causing chat messages to drop 1 line.
    3) Tapping "Add a caption..." opened the keyboard, but fixed `collapsedH` height and unresolving `WindowInsets.ime` left the caption bar trapped below the keyboard.
    4) View once icon was small (36dp seat, drawn ring only ~19dp).
    5) Selection bar sat below the grid in a vertical Column rather than floating over it.
  - Fix:
    1) AttachPanel Column background set to `Color.Transparent`.
    2) Grid fills `Box(Modifier.weight(1f))` and media thumbnails scroll directly underneath the floating bar (with bottom padding when selected).
    3) Selection bar floats over the grid (`align(Alignment.BottomCenter)`).
    4) Caption bar and Edit button styled with translucent dark rounded pills and 1.dp border for readability over photos.
    5) View once toggle enlarged to 44dp seat with 44dp CenteredOnceIcon (prominent and bold, matching Send button).
    6) Keyboard IME handling: `rememberImeGlidePx()` tracks IME height; `targetH` expands to `expandedH` when keyboard opens; selection bar padded by `imeGlideDp`, gliding smoothly above the keyboard when caption bar is focused.
    7) ChatScreen Composer condition changed to `else if (!showAttach || !attachFs)`: message bar stays visible in half panel when media is selected, preventing the 1-line chat drop.
- Version bumped to 212 (`3.9.135`). Gates: 41/41 (1672 assertions).

## Round 64 (owner: "end to end encrypted message system ta kore daw — call a ache ota; kono architecture jeno na vange; kono kichu na vange"; v213 / 3.9.136)
- E2EE MESSAGES (mirrors the call E2EE) done, worker commit `a3e8a5a` + app commits `d91f1e4`/`e970349`/`cba2985`/`0e8738d`, release `4af727f`.
  - Design (envelope, decided round-start): 1:1 only. Sender seals the body on the phone; the worker stays a faithful CARRIER — it stores/forwards the opaque envelope, never decrypts, never holds a private key (no crypto in the worker at all). Groups / AI / official stay plaintext automatically (they carry no key).
  - Crypto (app, new `E2eeMsg.kt`): standard JCA only — P-256 JCA KeyPair (API 24-safe, zero new deps), ECDH shared secret, HKDF-SHA256 (RFC 5869, pure-Kotlin over HmacSHA256, info "kp-msg-e2ee-v1"), AES-256-GCM (12B nonce, 128-bit tag). Envelope = "KP1." + base64(12B nonce || ciphertext+tag). Identity (priv+pub) generated once per install in private prefs "kp_e2ee" (msg_priv/msg_pub); a reinstall changes the key.
  - Public key path: new `users.e2ee_public_key` column (tolerant migration), served in every user shape via userFrom, published by the app once per process via `PATCH /api/me {e2eePublicKey}` (base64 ≤512 validated, in identityChanged fan-out), and in the chat-list freshness marker (a key change moves the marker so lists re-fetch).
  - Sealing (app): sendText TEXT, scheduleText, all media captions (5 payload sites), edit (decrypt → edit → re-seal), forward (source row arrives already opened; re-sealed per TARGET key looked up from ScreenStore.convs — group/bot target gets plaintext).
  - Opening (app): the envelope only ever exists in the payload/on the wire. ChatScreen unseals at every row entry — poll page, older page, WS frame (renamed let-param to rawMsg, `val liveMsg = unseal(rawMsg)`), paintSent, outbox echo. Pending optimistic echo stays plaintext. A row that cannot open (reinstall) renders a bare lock, never ciphertext.
  - Previews (worker): previewOf returns the lock for sealed TEXT; sealed media keep their labels (Photo/Video/Document) with the caption stripped. Chat list, push data, notification card and last_message all read only the lock. Envelope headroom: the 4000 plaintext cap becomes 8000 ONLY for "KP1." bodies on both send and edit (a full message seals to ~5.3k); plaintext cap untouched.
  - Verification UI (app): header lock row under the name/last-seen (solo, non-bot, peer key present) — same ceremony as the call: "End-to-end encrypted" → tap reveals the 3s safety code (E2eeCall.safetyCode on sorted pubkeys) → tap again opens the verify sheet; TOFU stored in prefs under "msgpeer_<id>" (separate from the call's DTLS TOFU); changed-key shows "Security code changed — tap to verify" + "Trust this new code".
  - Worker bug found+fixed while testing: syncPreviewAfterEdit rewrote last_message with the raw new body — for a sealed newest message that would have been ciphertext in the chat list; now uses previewOf (lock).
  - Honest limits (documented in E2eeMsg.kt): groups stay open (a group-key protocol is an architecture change the owner ruled out); media BYTES not encrypted this round (R2 object stays token-gated; captions are sealed); identity in private prefs not the keystore (reinstall ⇒ new key ⇒ peer's safety code changes, pre-reinstall messages show a lock, never a crash); server-side search cannot read sealed text (inherent).
  - Gates: 41/41 (1678 assertions), 68 app unit tests (7 new E2eeMsgTest crypto tests: round-trip both directions, stranger key, GCM tamper, full-length 4000, HKDF determinism, safety-code symmetry, encodings), CI green on the release SHA. CI taught three JUnit-4 lessons (no assertArrayNotEquals, no smart-cast after assertNotNull) — fixed in the test file.
  - Ops notes: (1) parallel edit_file calls to the SAME file race and clobber — this round lost two worker edits that way; after any "Context not found" on a same-file batch, re-audit ALL targets (grep counts) before continuing; (2) `node --import tsx test/cases/NN-*.mjs` is the fast single-case loop (plain `node` fails on the .ts worker import); (3) JUnit4 has no assertArrayNotEquals and its assertNotNull does not smart-cast.
- Version bumped to 213 (`3.9.136`). Worker `46707bb0-01a1-44f9-8726-4d2a1c424f0a` live at 100% (8 secrets), `releases/latest` = v213 (2 APK assets, release-only per owner 2026-09-22).

## Round 65 (owner feedback on v213: "shob thik ache" — 3 UI changes; v214 / 3.9.137)
- Item 1 (E2EE line placement) done, commit `fbd75bb`, app-only.
  - Owner: the header row under the username is not wanted ("okhan theke remove koro"); the E2EE line should be in 2 places — under the username in the profile, and at the start of the chat in the middle; the red-circled "Say hi, send a sticker or a photo" hint is removed, the E2EE line takes its place.
  - Fix: header `E2eeMsgCodeRow` block removed from ChatScreen. New homes: (a) profile — `E2eeMsgCodeRow(ctx, userId, u.optText("e2eePublicKey").orEmpty(), ...)` under `@$uname` in ProfileScreen (peer profiles only, !isMe; keyless accounts show nothing); (b) chat — a centered pill at the TOP of the message thread (`Box(align(TopCenter).zIndex(4f))` overlay + the LazyColumn top contentPadding grows 6dp→34dp to reserve its row, so it never covers a bubble and item indices are untouched); (c) empty chat — the centered EmptyState keeps "No messages yet" (note="") and the E2EE line sits in the middle where the hint was. All three reuse the same composable (tap → 3s code → verify sheet, TOFU warn unchanged).
  - New local vals: `e2eeOn = !isGroup && !botChat && e2eePeerKey.isNotBlank()` + `e2eePeerId` (declared after botChat — a VAL may not forward-reference, unlike a local fun).
- Item 2 (attach selection bar) done, same commit, app-only.
  - Owner: edit / caption / view-once "kono background nai" → 20% dim black; caption bar "arektu lomba"; view-once icon boro, tapped = blue border ring the size of the button.
  - Fix (AttachSheet.kt): all three controls now `.background(Color(0x33000000))`; caption bar 40→44dp height (22dp radius, in line with the buttons); view-once 44→48dp seat with `.clip(CircleShape)` + 20% black ground, `CenteredOnceIcon(48.dp)`, active state = `border(2.dp, ActionBlue, CircleShape)` ring the button's exact size (filled disc retired). Send untouched.
  - Test locks updated same-commit: r63-4 block re-pointed to the r65 design (48dp border ring, 44dp caption, 0x33000000), the big attach-panel block's CenteredOnceIcon/allOnce/height alternatives widened, the IME-glide block's list-padding pin now reads the conditional `top = if (e2eeOn && ...) 34.dp else 6.dp`, and the r64-e2ee app check now pins the r65 placements (e2eePeerId call, "Say hi" hint gone, profile placement).
- Ops notes: (1) the session restart wiped node_modules AND the .git directory (the .git credential exclusion removed the whole tree) — recovered with `git init && git remote add origin … && git fetch origin main && git update-ref refs/heads/main refs/remotes/origin/main && git symbolic-ref HEAD refs/heads/main && git reset`; local git identity re-set repo-local; push via `https://x-access-token:$GITHUB_PAT@github.com/…`. After any restart, verify `.git` exists before trusting git-based test cases (case 41 runs `git ls-files`). (2) The E2EE crypto core itself was confirmed working by the owner ("shob thik ache") — round 65 is placement/polish only, zero crypto changes.
- Version bumped to 214 (`3.9.137`). Worker unchanged (no redeploy). `releases/latest` = v214 (2 APK assets). Gates: 41/41 (1678 assertions), CI green on the release SHA.

## Round 66 — 2026-09-23 — IN PROGRESS
- Owner moved sessions after the previous session stopped. Latest feedback is preserved verbatim outside the repo at `/home/user/uploads/kuchupuchu-feedback-2026-09-23.md`. Read the feedback before implementing; do not guess product intent.
- Verified remote main: `bed9c362b527` (Round 65 records), latest release `v214` / `3.9.137` at `2de14d944e7a`; GitHub CI runs `35852487526` (main) and `35850858592` (release) completed success. The uploaded v211 handoff is stale; the repo's Round 64/65 memory is the current basis.
- Intake was source diagnosis only. Implementation and verification progress is recorded below; no Android device/emulator visual verification has been performed.
- Item 1: `ChatScreen.kt:3367` renders the E2EE line in a fixed TopCenter Box OUTSIDE LazyColumn; padding does not make it scroll. Move into the real thread with paging/quote-jump/scroll index handling. Current `e2eeOn` requires solo/non-bot/peer key; `sealOut` returns plaintext when keyless; `E2eeMsg.ensureOnce` publishes only on chat open and latches before success. Determine requested E2EE scope before changing security behavior. Do not fake an encrypted badge for plaintext chats. Existing media bytes and groups are NOT E2EE.
- Item 2: AttachSheet selection controls use `0x33000000` (20% black), separate caption + 48dp view-once seat, and an active blue border rather than blue icon. Owner wants a longer caption boundary containing the view-once icon and no extra icon border. Back (`ChatScreen.kt:4299`) immediately clears selections; reply, composer, sticker, and navigation paths have no shared deselect confirmation. Old test32 r45 locks explicitly forbid the confirmation; update the contract alongside the new owner-requested behavior, not just source.
- Item 3: `src/worker/index.ts:11185` replaces a sealed text preview with a lock; `ChatListScreen.kt:1279` displays it through friendlyPreview. This is a replacement message preview, not an independent decorative badge. Never restore server-side plaintext to remove the lock.
- Owner confirmed via ask_user: (1) all PERSONAL 1:1 chats only, group/media encryption scope unchanged; (2) 50% dim-black OPACITY, NOT panel height; (3) seen chat shows normal content preview, unread chat shows `(number) new message/photo/video/document/voice...`, no lock. Deselect confirmation applies to Back/reply/outside-panel actions; cancellation preserves selection.
- Next: resolve those decisions; implement each item separately with behavioral/source regression coverage and full gates; verify CI and release only after the intended paths are tested. Read all related source-lock tests before edits.
- Checkout is shallow; the initial sparse intake checkout was restored to FULL before verification. npm dependencies are installed (snapshot-excluded), git remote has no credential. Baseline gates passed: 41/41, 1678 assertions, ktlint/typecheck/prettier/secret-scan/Android validation. npm ci reported 6 pre-existing dependency advisories (1 low, 5 high); dependency changes are outside this UI/E2EE round.

- r66-1 implementation: E2EE notice is inside the first real/pending lazy row (only at true history start), never a fixed overlay; no extra lazy indices, so quote/album/paging offsets are unchanged. Identity publication starts at authenticated app entry, retries transient failures, single-flights concurrent calls and is acknowledged per session only after success. Derived peer-key state fixes long-lived callbacks capturing the pre-fetch blank key; unseal preserves the original envelope for late-key retries. Final Api.request transport guard covers text/caption sends from chat/outbox/scheduling/forward/share/status/notification and edits: unavailable keys defer with a retryable failure, never transmit plaintext; actual groups/bots and media bytes keep their existing scope. Wire crypto/KP1 unchanged for history compatibility.
- r66-1 verification: two new source regressions failed before and passed after; all gates 41/41 (1680 assertions); 18 JVM tests (11 new publication/send-policy + 7 existing crypto) passed. An extracted-VERBATIM E2eeMsg core was also compiled/run with lightweight Android/network ports: actual seal/open, missing/invalid key refusal, same-process publication retry, unchanged group/bot, late-key decrypt, edit guard all passed. Full Android build/CI still pending at this checkpoint. No worker change/deploy for item 1.

- r66-1 pushed: `6b37cd0`; CI `35874487829` completed SUCCESS (worker + Android tests/lint + release APK).
- r66-2 implementation: edit/caption backgrounds 20% -> 50% black; existing collapsed/expanded panel heights unchanged. A single wider caption boundary now contains the 48dp view-once control; active state colors the glyph blue with no extra border/disc. Only the attach use opts into full-seat icon geometry (47.68 x 46.70dp visible ink inside 48dp); other view-once icons unchanged. Caption height 52dp accommodates the icon; Send size unchanged.
- r66-2 selection policy: shared single-pending exit gate and Deselect/Cancel bottom sheet; Back/reply/composer/sticker/menu/call/profile actions cannot silently abandon the batch. An Initial-pass outside-pointer barrier consumes the whole outside tap/drag before prompting (modal-context semantics: a consumed gesture is NOT replayed into a call/record/send). Cancel retains selection/caption/HD/once state; Deselect clears and closes. Internal panel actions such as edit/send remain available. Existing r45 no-confirm source pins migrated because the owner's new instruction explicitly reverses that behavior.
- r66-2 verification: two new source regressions failed before and passed after; 8 pure JVM exit-gate tests passed (cancel preservation, deferred action ordering, repeated confirm, duplicate pointer/callback requests, fresh action after discard). Gates 41/41 (1682 assertions), ktlint/typecheck/format/secret-scan/Android validation passed INCLUDING the final header-action guards. Device interaction/visual verification still required.

- r66-2 pushed: `5b8959f`; CI `35876682386` completed SUCCESS (worker + Android unit tests/lint + release APK).
- r66-3 implementation: chat-list unread = count + accurate type (message/photo/video/document/voice/sticker/etc); seen = actual local plaintext/decrypted caption or the existing media label. No injected lock or raw KP1 text. Mixed/incomplete/large unread samples use an honest total `N new messages`. The worker carries an intact encrypted latest-row preview in list AND detail, never decrypts or stores a new plaintext preview. Existing latest-message queries were extended, with an exact indexed preview-timestamp seek; only unread chats inspect at most 32 recent metadata rows for classification (no full-history aggregation). No schema migration, extra D1 statement, new API route, or plaintext push payload.
- r66-3 privacy/cache: exclude deleted/reset/watermarked/expired previews; view-once exposes no caption/file pointer. Envelope + unread type + peer public key are in server/client fingerprints so encrypted edits and key changes invalidate correctly. A push clears stale preview metadata until authenticated refresh; decryption runs off Main and unread text is never displayed.
- r66-3 verification: 18 new regression assertions failed before and passed after; 41/41 (1700 assertions), all local gates; 9 new pure-JVM preview formatter tests passed. Existing list probe unchanged: six chats = 8 reads + 1 write; 13-member group = 8 reads + 0 writes; empty = 4 reads. `.scratch/` added to .gitignore so CI-monitor JSON cannot break formatting or be committed.
- r66-3 live verification: manually deployed worker `0361f24e-f717-46cf-8b79-4e65d54100d8`; 8 secrets retained; health 200. Before-deploy authenticated fixture proved preview field absent. After-deploy: seen opaque carrier intact, unread 1/message, 2/voice classification, mixed voice/photo => total messages, unauthenticated 401, mark-read => unread 0. All fixture users/sessions/conversations/members/messages were removed and remaining count verified 0. One sandbox-to-live list request was 1815ms; not a Bangladesh/device latency claim.
- Ops observation: worker deployments followed our app-only main pushes by ~32 seconds (latest before manual deploy was `6040a69e-1d3e-43bc-bdb7-37ab49af4c6e`, with no r66 preview code). Re-query deployments after pushes; do not assume a manually recorded Version ID stays current. Python's default urllib User-Agent got a health 403; curl/browser User-Agent and Node fetch both returned 200.
- Next: commit item 3 after final gates, verify key-change retry wakeup, bump/release v215 (3.9.138) from green CI, then record final live IDs and phone watch items. No v215 release exists yet at this checkpoint.

- r66-3 pushed: `fd95b3e`. GitHub initially rejected the push with Internal Server Error; fetched to confirm remote state, then retried the existing commit successfully (no rewrite/force/duplicate commit).
- r66-1 follow-up: a peer publishing its key must also wake the deferred outbox, not wait for reconnect/backoff. The existing profile event already invalidates conversation metadata; it now force-kicks the single-flight outbox after invalidation. One new source regression failed before the trigger and passes after. This is a retry-wakeup completion of item 1, separate from the preview commit; wire protocol unchanged.

- r66-1 retry follow-up pushed: `fd57f17`; local gates 41/41 (1701 assertions). Final release CI will cover it together with item 3 (superseded in-progress runs may be cancelled).
- Release candidate: v215 / 3.9.138. versionCode/Name and the release source pin advance together; signing/applicationId unchanged. Release-only CI artifact must be verified from THIS release SHA before creating the v215 tag/release. Publication is pending CI at this checkpoint.
- Phone watch list for v215: both personal-chat devices updated/opened; late/offline key publication and queued-send recovery; E2EE line scrolls away with the first message and returns only at actual history start; 50% caption controls + contained blue once glyph; selected media Back/outside/reply => Deselect/Cancel with Cancel preserving captions/once/HD; unread count/type -> seen local content with no injected lock. Consumed outside gestures deliberately do not replay a call/record/send. Reinstall/peer-key-loss history limits of KP1 and non-E2EE media bytes/groups are unchanged, not hidden by a fake badge.

- Final r66 E2EE UTF-8 audit found a pre-existing data-corruption path before release publication: `E2EE_BODY_MAX=8000` counted a 4000-character message as 4000 bytes, and send/edit sliced ciphertext. A real AES-GCM 4000-Bengali-character probe produced 16044 characters, stored only 8000 and could NOT decrypt. Fixed with a derived UTF-8/nonce/tag/base64 ceiling of 16044, returning valid envelopes unchanged and refusing oversized send/edit/schedule payloads instead of corrupting them. Plaintext 4000-character behavior and KP1 crypto/key format unchanged. Old source locks that required ciphertext truncation were corrected for this explicit integrity reason.
- UTF-8 proof after fix: handler probe input=16044, stored=16044, decrypts=true; full gates 41/41 (1705 assertions); combined actual-core/policy/formatter JVM run OK (36 tests, including new Bengali round-trip). Worker `179d230e-7420-47dc-a5e9-8a78ba65a7c9` manually deployed and live-probed: POST 201 with real GCM decrypt=true, edit 200, schedule 202 intact; oversize send/edit/schedule 400/400/400 with prior valid message unchanged. All fixture rows, INCLUDING scheduled_messages, removed and verified zero.
- Release candidate `055d9f9` / CI `35880755500` is superseded by this UTF-8 completion; v215 has NOT been published. Publish v215 from the final green SHA including this fix, still versionCode 215 / 3.9.138 (no tag/version reuse because it remains unpublished).

## Release hold — owner instruction 2026-09-23
- Owner explicitly changed the release policy: do NOT publish now or after each bug/round; fix the remaining bug list, verify it, then publish one final release ONLY after the owner explicitly approves. This supersedes the older automatic end-of-round release instructions.
- The previous turn was interrupted after the completed GitHub publish call. Verified via GitHub API in this turn: v215 is already public with two release APK assets, targeting 61468c0cee5568696258630d230e677c1a5c4478; release CI 35882914516 succeeded. APK manifest/version/signature and signing continuity were verified before that publish.
- No new release/deploy/push was performed while acknowledging the hold. Owner decision pending: retain published v215 or convert it back to draft to stop its discovery by releases/latest. Do not delete the release/tag or roll back production code without an explicit decision.
- Since v215 was publicly available, do not replace its APK with new code under the same versionCode. A later approved release must advance versionCode beyond 215 so already-updated devices can receive it.
- Continue fixing bugs one by one with verification and separate commits, but publishing is now an explicit owner-approved batch step. Also remember that production worker deployments were observed after main pushes; clarify deployment timing if the hold is intended to cover backend changes too.
- r67 (in progress, 2026-09-23): owner's post-v215 bug batch — (1) caption bar too thick + buttons must sync, (2) notification must decrypt on arrival and keep Reply ("same as old version"), (3) one message = one animation (a send is placed first and then re-flies from the bottom-right; send/sending/sent must not differ), (4) emoji reactions need haptics on BOTH phones and a rapid tap must replay immediately (the far side must mirror it exactly), (5) 50% dim background behind caption/send/view-once in the attach bar, (6) the four message tones 50% quieter. Each item lands as its own commit with the four gates; no release, no publish while the hold stands.
- r67 item 6 done in this commit: `KpSounds.MSG_VOLUME_TRIM = 0.5f` mixes send 0.60→0.30, sent 0.70→0.35, in-app 0.70→0.35, receive 0.55→0.275. The call/status/screen/reaction tones are untouched (the owner named exactly four). Case 42 pins the trim constant, the four call sites and the untouched sibling levels.
- r67 item 1 done in this commit: the attach selection bar has ONE height (`barH = 40.dp`) — the caption pill was 52 dp, the pen 40, the send seat 46 with a 44 dp disc, so the pill stood out as "mota". The pill is `.height(barH)` with `RoundedCornerShape(barH / 2)`, the view-once seat is pinned to `barH - 8.dp` (32 dp, `fillBounds` kept) so the glyph can never set the pill's height again, and the send disc is `barH` with a 20 dp glyph + 16 dp badge. The 50% dim ground and the borderless blue-on glyph are unchanged. Case 32's r39-6 / r63-4 / r66 assertions that pinned 48 dp / 52 dp / 44 dp were updated to the new expression (the guard's intent — one pill containing the glyph, no second ring — is kept).
- r67 item 3 done in this commit: ONE message = ONE flight. Root cause: the optimistic echo lives in the LazyColumn's pending block and the accepted row in the thread block, so the swap rebuilt the row's composition; v207's own-row rule (`mine && recent -> animates`, no FxArrivals gate) then flew the accepted row AFTER the message was already sitting in the chat. Fix: `FxFlights.claim(key)` (ChatFx.kt) keyed by the row's stable key (`clientId.ifBlank { id }`) — the echo claims the flight on the frame it is born and flies in (bottom-right for mine, bottom-left for peer), the swap finds it claimed and only takes the seat. `fxBorn` (live-birth predicate) and `fxEmoji` (v205: glyph plays once the row is no longer a sending echo) are separate, so the emoji rule survived. The pending items block got the same contentType as the thread block. Case 37's v200 shape lock and case 32's r31-12/r63-1 glyph assertions were updated to the new predicate names (fxEmoji for the glyph, fxFresh for the flight).
- r67 item 4 done in this commit: reactions mirror exactly. Client (EmojiAnim.kt): the 300 ms `lastTapMs` tap gate is GONE (it swallowed the second tap of a rapid pair, which is why the dance only came back "full animation complete hole"), every tap bumps replayKey so the running pass is thrown away and the glyph restarts from 0, and BOTH sides buzz through the new `Haptics.reaction()` (CONFIRM on API 30+, VIRTUAL_KEY below) — the tap and the replay that arrives from the other phone. Worker: the per-row 3 s dampener (`fxLastAt`) is deleted — it was what made the far phone show ONE pass while the tapping phone stuttered; storm protection is now the per-user token bucket alone (`rateLimit("fx:<uid>", 80, 180)` = 80 burst, 3/s sustained). Case 09's N3r block now asserts the second tap fans out again; case 42 pins the client + worker halves.
- r67 item 2 done in this commit: the message card decrypts on arrival and stays replyable. The worker now MARKS a sealed push (`kp_e2ee`) and rides the envelope itself (`kp_env`) while it fits an FCM data message (`E2EE_PUSH_ENV_MAX = 3_000`); a longer envelope is left out, never truncated. The lock preview became one named constant (`E2EE_PREVIEW`) and the system-drawn fallback card (no process of ours can decrypt for it) reads "New message" instead of the lock. On the phone, `PushSeal.kt` decides (kp_e2ee / envelope-in-payload / older worker's plain preview), resolves the peer key ONCE up front (cache -> ScreenStore -> GET /api/conversations/{id}; a keyless phone makes no request), and opens either the pushed envelope or the pushed row from its own conversation's newest page — the page the open chat already fetches. `openBounded(..., 3_500L)` caps the fetch path so the FCM handler always posts a card inside its budget (a hung fetch costs the text, never the card). `cardText()` is the single gate for card text: opened plaintext, else a neutral label — never ciphertext, never a bare lock; both the background card and the foreground list preview go through it. `KpNotify.message` sets BigTextStyle for a text card so a long message is readable in full from the shade. Reply / Like / Mark-as-read are unchanged and a reply still leaves sealed by the transport guard. Case 42 pins the markers, the label rule, the bounded fetch and both call sites; case 32's r33-12 / r64-e2ee pins were migrated (the escaped-lock pin must carry a literal `\\uD83D\\uDD12` in the .mjs). The .scratch JVM probe (PushSeal verbatim + lightweight ports) covers the decision table, the single key resolution, the offline path and the timeout: 19 ok / 0 broken.
- r67 item 5 done in this commit: the whole attach selection bar sits on the 50% dim ground. Round 66 (and an earlier round, for the pen) gave it to the caption pill and the pen only; the view-once seat and the send seat were bare, so half the bar floated over the grid. The view-once seat (`barH - 8.dp`, inside the pill) now draws its own 50% circle, and the send seat (`barH + 4.dp`) draws it as a SHAPE — `background(Color(0x80000000), CircleShape)`, never `clip()` — because the count badge deliberately overhangs that box and a clip would slice it. Heights are untouched (r67-1's single `barH` stands). Case 42's r67-1 send-seat pin was migrated (the seat now has its own modifier line) and case 32's r66-2 count pin now guards all four grounds instead of two.
- r67 verification (all six items pushed): CI `35893703196` on the tip `85352e2` completed SUCCESS (jobs: Worker typecheck/tests/format/secrets, apk). The earlier per-item runs (`35890659107`, `35891118901`, `35891878400`, `35892144538`) were CANCELLED by the next push, which is the usual supersede-by-concurrency behaviour — the tip run covers the whole tree.
- r67 worker deploy: items 4 + 2 changed `src/worker/index.ts`, and the Cloudflare integration auto-deployed them (newest deployment `2026-09-23T17:10:13Z`, version `1bc32b54-215...`, after push `76eee70`). Verified against the DEPLOYED bytes (scripts content/v2): `kp_env` x3, `kp_e2ee` x2, `E2EE_PUSH_ENV_MAX` x2, `E2EE_PREVIEW` x3, `fxLastAt` 0 (the r67-4 dampener really is gone). No manual deploy was needed and none was run (release hold respected).
- r67 live probe (`.scratch/live_r67_push_probe.py`, 10 ok / 0 broken, isolated fixtures removed with a zero-row assert): health 200; a short sealed body sends 201 and comes back byte for byte; a sealed body LARGER than `E2EE_PUSH_ENV_MAX` also sends 201 and is stored INTACT (the push copy is skipped, never truncated); the peer fetch matches; the list row stays `unread 2 / unreadPreviewKind "message" / lastMessage 🔒` — never ciphertext; the edit path keeps the same headroom.
- Phone watch for the r67 batch (owner device): notification shows the real text and Reply works from the shade, incl. a long message expanded; a killed-app push still makes a card; send/sending/sent animate as ONE arrival (nothing pre-places); a rapid emoji tap replays same-to-same on BOTH phones with a buzz on both; the attach bar reads 40 dp with all four controls on the 50% ground; the four message tones are audibly half.
- r68 (2026-09-24, owner's round after the r67 batch; items 1/2/3 feedback + 7/8 are new): 1 caption bar position, 2/3 confirmed fixed, 4 the emoji haptic must only fire when BOTH users are on that chat screen, 5 one shared rounded background for the whole attach bar + it animates in from below, 6 "50% 40% kore daw" = the message tones go to 40% (owner-confirmed: sound, not the bar dim), 7 full-chat delete gets a checkbox popup (tick = gone from BOTH sides permanently, untick = only mine, the other user keeps everything — owner-confirmed), 8 message delete = ONE option; in a 1:1 both own and opponent messages go through the same popup and the opponent's message can be deleted for everyone (owner-confirmed: same checkbox popup as 7). Items 7/8 wait for the two screenshots (they did not arrive in the workspace; the owner said he will re-send them).
- r68 item 1 done in this commit: the caption bar sits on the keyboard. The panel Column carried `.imePadding()` AND the selection bar added `.padding(bottom = imeGlideDp)` — two lifts, so the bar floated a full keyboard too high and in the 40% panel it went clean out of the top (the "half attach screen a caption bar dekhai jai na" report). Now exactly one lift: the panel pads by the shared glide value the composer rides (rememberImeGlidePx; `WindowInsets.ime` does not resolve on this BOM), the bar keeps none, and both grids reserve the bar's real height (76 dp) instead of `68.dp + imeGlideDp`. Padding the panel (not its height) is what lands the content's bottom edge on the keyboard's top edge. Case 43 (new, r68) pins it; case 32's r39-6 pin migrated.
- r68 item 4 done in this commit: the mirrored reaction only fires when BOTH users are on that chat screen. The far side's replay (animation + buzz) was enqueued whenever the socket frame arrived for a live chat screen — and a backgrounded app keeps its composition alive, so a pocketed phone buzzed for an animation nobody could see (same for a chat screen buried under the media viewer). The decision is now the pure `EmojiFxPolicy.mirrorsOnScreen(foreground, route, convId)` (boundary-safe `chat/<id>` / `chat/<id>?` match, the same shape the notification path uses), and `emojiFxReplays.add` has exactly one call site behind it. The local tap still buzzes and still posts /fx on every tap, so rapid taps keep mirroring same-to-same while both are watching. EmojiFxPolicyTest (6 cases) + .scratch JVM probe (9 ok) cover the table; case 43 pins the gate.
- r68 item 5 done in this commit: the attach bar is ONE capsule that rises from below. The four per-control fills (pen circle, caption pill, view-once seat, send seat — r66/r67-5) are replaced by a single `background(Color(0x80000000), RoundedCornerShape(barH / 2 + 8.dp))` on the row, drawn as a SHAPE (not a clip) so the count badge keeps overhanging the send seat; 8 dp outside + 8 dp inside = the 76 dp both grids reserve below their last row. `AnimatedVisibility(slideInVertically + fadeIn / slideOutVertically + fadeOut)` gives the entrance/exit. The pill outline and the blue send disc stay. Case 43 pins it; case 32's r39-6/r66-2 and case 42's r67-5/r67-1 pins migrated from four fills to one ground (the merge is by owner design, not a regression).
- r68 item 6 done in this commit: the four message tones are at 40% of their original mix (send 0.6→0.24, sent 0.7→0.28, in-app 0.7→0.28, receive 0.55→0.22) — still one constant (`MSG_VOLUME_TRIM = 0.4f`) and the same four call sites; reaction/call/status/screen tones untouched. The owner confirmed item 6 meant the message sounds, not the attach bar's 50% dim (which stays at 0x80000000). Case 43 pins the constant + the four sites; case 42's r67-6 pin now accepts 0.4 or 0.5 so the round guard states the guarantee (one constant, four sites) instead of a number.
- r68 item 7 done in this commit (`b4e9c66`): the full-chat delete asks with a checkbox popup. The old delete was one silent step that always wiped BOTH sides; it is now the owner's popup (peer avatar + "Delete Chat", "Permanently delete the chat with {name}?", checkbox "Also delete for {name}" default UNTICKED, Cancel / Delete Chat), built as ONE shared composable (`Ui.KpDeleteDialog`) that item 8 reuses. The checkbox is the whole decision and the SERVER makes it per member: TICKED = both members watermarked, rows deleted (media objects swept), preview + both unread counters cleared, room hears `{type:"cleared"}`, both lists re-read; UNTICKED = only the caller's watermark moves (rows stay, the other side keeps every message, its preview and its counters) and a newer message brings the chat back with only what is new. Every watermark consumer learned the mine-only case: `/api/search` message rows now carry `c.hidden_json` and are filtered by the caller's mark, `/api/conversations/:id/media` is filtered the same way, and `GET /api/conversations/:id` answers `hiddenByMe` (new `convHiddenFor`) so the chat list's one-conversation poke merge drops such a row instead of re-upserting the chat that was just deleted. Groups keep the old rule (the delete is a leave; `forEveryone` comes back false). The owner's semantics were confirmed before implementing ("হ্যাঁ, এটাই" — unticked = only my copy). Tests: case 02 section 1/3 now drives the ticked path (and its new 3b block covers unticked: list, history, search, media, hiddenByMe, reopen), case 20's paging watermark uses the ticked delete, case 32's r32-12/r38-1 now land on the popup, and the new case 44 (worker half) was split across the two item commits.
- r68 item 8 done in this commit (`26717e1`): ONE Delete everywhere, same checkbox popup. The two rows ("Delete for everyone" / "Delete for me") are gone from the long-press sheet, the multi-select bar, the photo viewer, the clip player and the document viewer; the single Delete opens the same popup ("Delete message" / "Are you sure you want to delete this message?" — plural for a multi-select), and its answer splits the work: ticked = the per-id server DELETE for the rows that exist (a sending echo has no id and leaves locally either way), unticked = the pre-existing local hide (`deleteForMe`). Ticking now reaches the OTHER person's message, which the owner asked for, so `/api/messages/:id` lets a member of a PERSONAL chat delete a message they did not send; groups keep 403 and the two bot accounts stay sender-only (their transcripts are the product). The gate is one predicate `canDeleteForEveryone(convId, m)` (not an echo, not a group, other member is a real person — own messages were always deletable). The clip player / document viewer are ROUTES with no conversation, so ChatScreen publishes itself (`ScreenStore.activeConvId` / `activePeerName`) and they read the label + gate from there; the generic photo viewer takes the label as a parameter. Tests: case 44's item-8 half (peer delete in a 1:1, bot refusal, group 403, own delete still allowed, the one-Delete UI, the split answer, the viewer wiring), plus migrated superseded pins in case 32 (r17-13/r31-8, r31-29 two→one `selected.addAll(albumIds)` sites, r32-16, r32-46, r32-40) and case 36 (fb#8, fb#8 routing) — and case 43's header now points 7/8 at case 44.
- r68 7/8 compile fixes (`b79e659`, `167d52c`): the first CI run that compiled this batch surfaced TWO Kotlin errors that the per-item runs had never reached (superseded by concurrency), both already-fixed classes. (a) AttachSheet.kt's r68-5 bar animation resolved the ColumnScope `AnimatedVisibility` through the outer implicit receiver while sitting in a Box that needs `Modifier.align` → the top-level overload is now named explicitly. (b) `deleteWallChat` was a local fun inside the wall's `if (blockWall)` block, below the popup that calls it (Kotlin resolves locals by source order) → the DELETE moved to the screen's own scope with `deletingChat` state; the wall's pill only raises the popup now. Case 32's r38-1 pin follows the move. Lesson kept: after ANY Kotlin batch, the only real compile witness is CI's `testDebugUnitTest lintDebug` — local gates (ktlint/validate:android/prettier) do not compile.
- r68 7/8 verification: tip `167d52c`; CI `35949672522` completed SUCCESS (jobs: Worker typecheck/tests/format/secrets, apk — the APK job compiles Kotlin + lint + unit tests, so the popup batch is compile-verified now). Worker auto-deployed after the pushes: newest deployment `2026-09-24T03:01:47Z`, version `d6c95508-840` (deployed bytes 334 566, markers `forEveryone` / `convHiddenFor` / `hiddenByMe` present; the minifier drops most comments, so marker counts are code-only). Live probe `.scratch/live_r68_delete_probe.py` against the DEPLOYED worker: 17 ok / 0 broken (health; a 1:1 peer-delete 200 and the row gone from both histories; a GROUP peer-delete 403; unticked delete = B's list loses it while A keeps it, hiddenByMe true for B / false for A, search finds it for A and never for B; ticked delete = both hiddenByMe true, rows 0, preview cleared; the group delete still a leave; all fixtures removed with a zero-row assert).
- Phone watch for r68 7/8 (owner device): the chat-delete popup matches the screenshot (title + peer photo + question + unticked checkbox); untick → the chat leaves MY list and device but the other phone keeps everything; tick → both phones lose the chat and neither comes back with old messages; ONE Delete in the long-press sheet, the multi-select bar and every viewer, opening the same popup; in a 1:1 either side can tick and take the opponent's message away; a group cannot delete a member's message (403 → the app hides nothing).
- r69 (2026-09-24, owner's list after testing r68; all items NEW in r68): items 1/2/3/5/6 confirmed fixed; 4 "maybe not fixed (only chat screen a thaklei hobe eita)"; 7/8 semantics accepted but the popup box looks fat ("left right a jaiga rekhe mota keno korla"); plus a NEW crash (attach → tap a selected media → `IndexOutOfBoundsException: index 0, size 0`, route `chat/c_0ad0a29d...`, 2026-09-24T03:16:20Z, AttachPanel:452 ← ChatScreen:1205), item 9 (muted chat still notifies and still rings; the chat screen shows no mute sign — put one icon by the name; pressing Mute must offer TWO answers, call mute / message mute, and only the chosen half mutes) and item 10 (swipe-right delete deleted with no popup; standing rebuke — check EVERY button/screen, no shortcuts). Release hold stands; v215 stays published.
- r69 crash fix (`c5e537d`): `AnimatedVisibility` composes its content one more time while it animates OUT, and by then `sel` is empty — the bar read `sel[0]` for the caption and `sel.size` for the count badge. The bar now paints a MIRROR of the last real selection (`exitCount` / `exitCaption` / `exitOnce`, written in `SideEffect` after composition) and reads `barCount` / `barOnce` / a guarded caption write. New case 45 pins the guard; case 32's r39-6 badge pin reads the mirror.
- r69 item 4 fix (`30c996e`): `EmojiFxPolicy.mirrorsOnScreen(route, convId)` — the app-level foreground flag is no longer part of the decision (it could only ever SUPPRESS a mirror that should have played). Being on `chat/<id>` (or `chat/<id>?...`) is the whole condition; EmojiFxPolicyTest / case 43 / case 45 pin it.
- r69 items 7/8 fix (`6146878`): the delete popup is measured, not padded — `DialogProperties(usePlatformDefaultWidth = false)` + 16 dp side margins (the platform default width was what made it fat), 10 dp corners, 24/24/14/12 padding, 34 dp avatar, 21 sp Bold title, 16 sp body, 20 dp checkbox seat in a 56.4 dp band, buttons 16 sp SemiBold 12.4 dp off the bottom — all from the owner's screenshot at 2.748 px/dp. Same public params, so every caller (message delete, chat delete, block wall) gets the same box; case 44 pins the geometry.
- r69 item 10 fix (`7df5312`): `ChatDeleteDialog` + `deleteChatsNow(scope, ids, alsoForThem, after)` are the screen's shared pair; BOTH chat-list swipe delete slots raise the popup (they used to delete on the spot) and the 190 ms vanish now runs on the answer. Five entry points counted and pinned (thread ⋮, chat-list row, chat-list multi-select, block wall, both swipe slots).
- r69 item 9 fix (`012e38a`): the mute has TWO aspects. Worker: `members.muted_call` / `muted_msg` (migration), the mute route takes `{call,msg}` (one half at a time) while a bare `{muted}` still flips both and `muted` stays the OR for older readers; a CALL-muted member is not rung by the 1:1 ring, the group ring or the missed-call card (no relay, no push — the call row stays RINGING so the caller times out normally); the message push now carries the MESSAGE half (`muted_msg`), so a call-only mute no longer silences messages; the 1:1 call payload and the ring push name their conversation (`conversationId` / `kp_chat`) so the phone can re-check the mute it was told about after the fact. App: one shared `KpMuteChooser` (Ui.kt) opened from EVERY mute entry point (chat ⋮, the AI chat's menu, the group menu, the chat-list long-press sheet — via the screen-level `ListSelect.muteFor`, both swipe slots — `askMute`, the profile menu); the chat header draws a small bell-off beside the name (Muted / Calls muted / Messages muted) and drops both call buttons while calls are muted; silence follows the message half (`isSilenced = isMsgMuted || isHidden`) and a muted chat posts NO card at all (the old path only moved it to the silent channel, which still drew one); the push handler + the engine's own `/active` poll both refuse to ring a call-muted chat. case 45 (new) drives the real worker for both halves plus the legacy body and half-unmute; case 32's mute pins (r32-5a, r32-5b, r32-38, r32-6, r32-12, r31-26, r33-6) migrated.
- r69 compile fix (`0c1becf`): CI 35954500687 (``apk``) failed with TWO "This annotation is not repeatable" errors — the r69 edits inserted a new composable BETWEEN an existing `@Composable` line and its function, so the orphan annotated the new declaration and `KpSheet` / `ChatRowSheet` lost theirs. Same class as r68's CI-only failures.
- r69 guard (`4ecf96d`): new case 46 checks the Kotlin source SHAPE the sandbox cannot compile — brace/paren/bracket balance for all 97 app sources (comment/string/backtick-aware: a JUnit name with an apostrophe used to fake an imbalance) and no DETACHED annotation (a `@Composable` whose next non-comment line is not a declaration). Verified against the real bug: re-injecting the orphan makes case 46 fail; removing it passes.
- r69 fixtures (`d7780a0`): CI 35954500687's Worker job also broke four r68-7 unticked checks. Root cause was a FIXTURE race, not the app: an unticked delete writes a TIME watermark at "now", and the round-13 data-loss guard keeps a row whose `created_at` EQUALS that instant visible — on a fast runner the delete shares the last message's millisecond, that row survives and revives the chat. Cases 44 and 02 now age their TEXT rows + `last_message_at` by 90 s before the unticked delete (the photo fixture already did).
- r69 verification: tip `d7780a0` == origin/main; CI `35956120661` SUCCESS (Worker + apk). Worker auto-deployed: version `dcbdbbc5-...` (#843, 2026-09-24T04:33:23Z), deployed bytes 338 735, r69 markers live (`muted_call` ×12, `muted_msg` ×8, `kp_chat` ×9, `mutedCall`/`mutedMsg` ×2) with the r68 markers still present. Live probe `.scratch/live_r69_mute_probe.py` against the DEPLOYED worker: 15 ok / 0 broken (health; `{call:true}` stores/reports the call half only and the other member is untouched; a message still lands in a call-muted chat and reaches the muted member's list with unread; `{msg:true}` flips the message half and clears the call half; a single-half body never zeroes the sibling; legacy `{muted:true}` / `false` flips/clears both; `members` really carries the two columns; the r68 unticked delete + `hiddenByMe` semantics still hold; zero fixture rows left).
- Phone watch for r69 (owner device): attach → select media → tap the media (no crash, the bar's exit is clean); the emoji reaction only buzzes/mirrors while BOTH phones are on that chat screen (leaving the chat, opening the media viewer or a pocket must stay silent); the delete popup box matches his screenshot (narrow, 16 dp side margins, same checkbox); swipe-right delete now asks the popup (both swipe slots, main list and archive); Mute offers exactly two answers and the chosen half is what mutes — a call-muted chat is silent for calls but its messages still arrive with sound, and a message-muted chat still rings; the chat header shows the small bell-off by the name (and no call buttons) while calls are muted.
- r70 (2026-09-24, owner's list after testing r69): items 1/2/3/5/6/7/8/9/10/11 confirmed fixed, item 4 NOT fixed ("peer chat screen a na thake, tobu amar phone e buzz kore" — confirmed against the options), plus five new items 12–16. Release hold stands; v215 stays published.
- r70 item 13 (`266ea0c`): a reply is born a reply. The quoted id was written to the POST payload ONLY, so the optimistic echo was born unquoted and BECAME a reply when the server row replaced it (the quote block grows the bubble a beat after the send landed). One `replyId` per send, written to the payload AND the echo; the reply bar is consumed once. The same hole ran through the media paths and was fixed in the same commit: `sendImage` (echo + live payload + send-later payload), `sendFile` (echo, captioned payload, and the uncaptioned arm now passes an explicit `plainPayload()` because `Uploads.sendFile` built its own body without the quote), and `scheduleText` (same one-value shape). A batch consumes the quote on its FIRST item, so the bar closes once.
- r70 item 14 (`db99603`): the caption bar is slimmer, longer and lighter — `barH` 40 -> 34 dp with the pencil seat riding the same constant (it was the last 40 dp literal), side air 10 -> 4 dp, ground 0x80000000 -> 0x66000000 (still ONE capsule), and both grids now reserve the row's real 70 dp of ink. The r67-1 / r67-5 / r68-1 / r68-5 / r39-6 / r66-2 pins in cases 32, 42 and 43 were migrated (their guarantee — one constant, one capsule, the real reserved height — is unchanged).
- r70 item 15 (`ab4b9a0`): the pencil's round seat wears the FIRST ticked item's thumbnail (process LRU then ThumbDecodeGate, the grid's own decode path) with a 16 dp pencil over a light scrim; the tap still opens the LAST ticked item in the editor. It answers "edit button tar rounded circle background a selected phone/video ta dekha jabe, multiple holeo first ta".
- r70 item 16 (`253c2e0`): the media editor's white glyphs vanished over white media. The rail's `StageHistory` seats keep the bare glyph v170 asked for and gain `Modifier.shadow(8.dp, CircleShape)`; the top bar's ✕ and the undo/redo pair take the same halo at 6 dp (sizes 36/32 untouched, so case 32's v169 counts still hold).
- r70 item 4 (`026d7f6`): the mirrored reaction needs BOTH phones on the chat screen. Two holes: (a) `Store.route` was a stale claim — the chat screen wrote it on entry and cleared it only from its own back arrow / delete, so a chat buried under the media viewer / clip player / doc viewer or a backgrounded app still reported `chat/<id>`; there is ONE writer again (KpApp observes the nav back stack: a chat entry -> `chat/<id>`, anything else -> its nav pattern, no entry -> ""). (b) the "both" half now has two sides: this phone's = `Store.foreground && mirrorsOnScreen(route, convId)` (the pair the notification path has always used), the tapper's = `onChat` posted with /fx, relayed by the worker as `fromChat`, required by the receiver (a body-less tap relays `fromChat:true` = old behaviour). r69 had dropped the flag on "only chat screen a thaklei hobe"; r70 put it back. Case 09 drives the three frame cases; case 47 pins the one-writer route + the gate; cases 43/45 pins migrated.
- r70 test fix (`8e1974c`): CI 35973792920 failed the Worker job on case 02 (2 broken) while the suite was green locally three times. The run-level logs endpoint works (`/actions/runs/{id}/logs` returns a zip; the JOB-level endpoint 401s): the twin-delete block saw the chat survive its own second unticked delete and its history come back as ["two","three"]. Cause = the same watermark race the r69 fixture commit fixed elsewhere: an unticked delete writes a TIME watermark at "now" and the round-13 guard keeps a row created at that exact millisecond visible on purpose (case 02 section 6 asserts that on purpose). Case 02 gained one shared `ageRows(cid)` helper (rows + preview 90 s into the past) used before the twin-delete and paging deletes; case 17's hide-one-chat fixture had the same latent race and was aged the same way.
- r70 verification: tip `8e1974c`; CI `35975216740` SUCCESS (Worker + apk); local suite 47/47 cases / 1836 assertions. Worker auto-deployed: version `c96fb0fb-ae05-45d2-be49-398c5a7d2d6c` (#846, 2026-09-24T08:26:29Z), deployed bytes 338 829, markers `fromChat` ×2 / `onChat` ×2 plus the r69 mute markers. Live probes against the DEPLOYED worker: `.scratch/live_r69_mute_probe.py` 15 ok / 0 broken (no r69 regression) and `.scratch/live_r70_fx_probe.py` 7 ok / 0 broken (the fx route reads the new body, still 200 + replay, a body-less tap keeps the old behaviour, a non-emoji KIND still 400 NOT_EMOJI, a stranger 401; zero fixture rows left).
- r70 item 12 (NOT implemented, preview only): the owner wants an animated EYE icon beside the name when both users are on the chat screen — "age preview dekhaba tarpor agei add koro na". `previews/r70-item12-presence-eye.html` (workspace root, outside the repo) shows three variants (blink+pulse, gaze sweep, ripple ring) in a phone mock-up, with an online/away toggle, the placement notes and the honest metadata caveat (presence is a server-side flag, so it is not E2EE; 1:1 only unless he says otherwise). Waiting for his pick before any code.
- Phone watch for r70 (owner device): a reply keeps its quote from the first frame (text, photo, video, document) and the reply bar closes; the caption bar is slimmer/longer/lighter with the first photo's thumb in the edit seat; the editor's white buttons are visible over a white photo; the reaction dance + buzz reach the other phone only when BOTH are on the chat screen (background it, open the media viewer, or leave the chat and it stays silent); send/sending/sent still flies once.

## r75 — the voice recorder is the MD's system (2026-09-25)
- Owner's order: "current voice lock system hold swipe up system shob remove koro ami ekta md file diyechi dekho ei vabe hobe shob" — the r71-r74 Telegram-style lock (tap-to-lock, lock-on-any-up-release, parked badge, white-glass Send) is REPLACED by the attached `voice_recording_interaction_analysis.md` model. Clarified with the owner BEFORE coding: the view-once double-tap on the locked Send STAYS (his pick), single tap sends normally.
- r75-1 (`dbac61a`): the MD's state machine. The lock fires MID-DRAG the instant the finger crosses 96 dp into the capsule (HoldMicButton calls lockRecording from the gesture; the release afterwards is inert — the MD's "ACTION_UP must not stop it"); the capsule is a dark rounded VERTICAL capsule (40x56, RoundedCornerShape(percent = 50), white lock icon, "Lock recording") that slides with the finger and pulses 1.0 -> 1.08 with the accent border when armed; NO badge once locked (the toolbar has none); slide-left past 88 dp still cancels (red trash); a plain hold-and-release SENDS; the mic follows the finger on both axes (animX/animY); the tap-lock + lockPending/recStarting queue are deleted (a take is always born a HOLD); the live wave moved into the locked toolbar — the HOLD keeps the dot, the clock and "‹ Slide to cancel". VoiceHoldGesture.decide is LOCK/CANCEL/SEND (no ms/slop); the JVM VoiceHoldGestureTest and case-47 section 19 were rebuilt around the new rules (the dropped `const ui` broke the seat-window check once — fixed by keeping the declaration).
- r75-2 (`4f233cd`): the owner's palette. Locked Send = the SAME accent circle + AmberInk paper plane as every other send (19 dp; white glass 0xE6FFFFFF and the chevron gone with the DoubleArrow import); the lock capsule and the Pause pill sit on the theme's DarkCard (hardcoded 0xE614181F gone); delete stays Red; MD content descriptions ("Send voice message", "Pause recording"/"Resume recording", "Record voice message", "Delete recording", "Lock recording").
- Gates per commit: 47/47 cases / 1892 assertions, tsc --noEmit, prettier, ktlint, secret-scan (r75-1). Worker untouched — no deploy. Release hold stands (owner 2026-09-23): NO new release/publish without his explicit approval; the next one must be versionCode > 215.
- Phone watch for r75 (owner device): press-and-hold = instant record with the capsule above the mic; drag up ~1 inch — the capsule lights + pulses and the take locks WHILE the finger is still down; lifting the finger then changes nothing; the toolbar (bin / clock / wave / Pause / Send) is up; Delete throws the take away (bin-drop animation); Pause really pauses (clock + wave freeze); single-tap Send sends; DOUBLE-tap Send sends view-once; slide mic left = cancel; a plain hold-and-release sends; a sub-second send says "That voice note is too short." only if it was locked.

## r75-3 — the recorder re-aligned to WhatsApp's frames (2026-09-25)
- Owner sent `voice_recording_all_frames_contact_sheet.jpg` (≈370 frames) and ruled: "frame by frame dekh eita WhatsApp er voice system ar tui jeta banaichia puray faltu kono alignment nai". Frame read: HOLD = bare bar (clock left + white "‹ Slide to cancel", mic planted, small capsule attached right above it); DRAG = the capsule rides the finger 1:1 while the mic stays put; LOCKED = a dark TWO-ROW panel — (clock · waveform · lock-circle) over (bin circle · wide Pause pill · round pink Send).
- r75-3: locked state is now that panel (DarkCard, 24 dp corners; lock circle/trash/pause on 8% white seats; Send = accent circle w/ the view-once double-tap, fxMicAnchor kept); the composer's right slot renders NOTHING while locked (`when { locked -> Unit ... }`, the old `|| locked` gating and the seat's locked branches are gone); the HOLD strip is bare (clock + Muted hint, PulsingDot deleted, wave moved into the panel); the capsule sits at -48 dp and tracks dragY 1:1; the mic follows x only (animY deleted). The r71-20 `// r71-20` comment must stay INSIDE the seat's `when` (SEAT_DOUBLE_TAP regex pins it there).
- Sandbox lesson (bit twice): `.git` does NOT survive the turn boundary in this workspace — before any commit, re-graft: clone to /tmp with the PAT, `mv /tmp/kpgit/.git /home/user/KuchuPuchu/.git`, re-set repo-local identity; the working tree survives untouched. Also `node_modules` is dropped each turn: `npm ci` (~90-150 s) before the first test run.
- Gates: 47/47 cases / 1892 assertions, tsc, prettier, ktlint, secret-scan. Worker untouched. Release hold stands (owner 2026-09-23); next release versionCode > 215.
- Phone watch for r75-3: locked = the wide dark panel with the big Pause pill; hold = bare bar + capsule tight above the mic; capsule rides the finger all the way up; mic never jumps; bin / Pause / Send all inside the panel.

## r75-4 — the HOLD state matches the owner's WhatsApp screenshot (2026-09-25)
- Owner (screenshot of WhatsApp mid-hold): "ekhon voice lock system a shob remove kor ami jemon jemon step by step bolbo temon korbi ... hold kore rakhle kemon hoi ... er por ami lock system dekhabo age eita dekh just" — this round is the HOLD VISUAL ONLY; the locked-panel review comes next on his word. Screenshot read: a tall dark column rises from behind the mic (white lock on top, small ^ chevron under), and ONE dark rounded bar carries the clock (left) and the grey centred "‹ Slide to cancel".
- r75-4: LockBadgePill -> 36x100 dp column (DarkCard, 18 dp corners, TopCenter; lock 17 dp white, +10 dp, chevron 18 dp at 60% white; accent border ONLY when armed — else transparent; pulse 1.0->1.08 kept); offset -50 dp (bottom tucked behind the mic) + lockDragY 1:1 (unchanged); the hold strip is one DarkCard bar (22 dp corners, 14/13 padding), clock Ink 13 sp left, hint centred via a weight-1 Box, Muted. KeyboardArrowUp import returned (Language < KeyboardArrowUp < Lock order).
- Gates: 47/47 / 1892 assertions first try, tsc, prettier, ktlint, secret-scan. Worker untouched. Release hold stands; next release versionCode > 215. Pending: the owner will review the LOCKED panel next ("er por ami lock system dekhabo").

## r75-5 — the locked panel is the owner's screenshot (2026-09-25)
- Owner (screenshot of WhatsApp locked recording, his app's colours): "eita lock kore thakle emon vabe record hobe shob buttons wave delete send shob kichu valo kore notice kore dekho. ager ta ami ekhono check korini ekbare check korbo" — the HOLD review is still pending on his phone; this round was the LOCKED panel only.
- Screenshot noted control by control: top = clock + wide grey wave + a "1" (view-once) circle; bottom = red bin on dark-red seat + wide Pause pill + big accent Send with a DARK double chevron. NO lock glyph in the panel.
- r75-5: panel 28 dp corners; clock 17 sp; wave weight-1 x 26 dp GREY (Muted) with LIVE_BARS 40 -> 64 (dense bars, 6 s window); the "1" circle = REAL view-once toggle (36 dp, CenteredOnceIcon, accent ring armed, onToggleVoiceOnce + haptics; voiceOnce state in the chat, reset in finishRecording; Send wire = finishRecording(once = voiceOnce); double-tap still forces once, r71-19b); bin 44 dp on Red 15% seat (glyph 20 dp); Pause pill 23 dp corners, 12 dp padding, glyph 17 dp + label 15 sp; Send 48 dp accent, DoubleArrow 20 dp tint 0xFF10141A (import back). The old lock circle ("Recording locked") is gone.
- Lesson (again): a python splice that ENDS with its own end-anchor text duplicates it — ktlint's parser caught the double `} else {` (ChatScreen 5367) BEFORE push; always make the anchor NOT overlap the replacement tail.
- Gates: 47/47 / 1892, tsc, prettier, ktlint, secret-scan. Worker untouched. Release hold stands; next release versionCode > 215. Pending: owner's phone review of r75-4 (hold) and now r75-5 (locked).

## r75-6 — the swipe locks again (2026-09-25)
- Owner (screenshot of MY hold state at 0:03 + his "1" reference): "wtf voice wave koi delete button koi send button koi? reference er sathe to 10% o match koreni" — the panel only shows AFTER a lock; his screenshot proves the take was still a HOLD at 3 s, i.e. his swipe never locked. He skipped the clarify question; treated as "lock is broken on-device".
- Root cause: HoldMicButton read `change.positionChange()` = the UNCONSUMED delta. Any parent winning an earlier pointer pass (the composer sits under screen-level handlers) eats the change first — dragY accumulated ~0, the threshold never crossed, and the release decided SEND (voice note went out). Fix: `positionChangeIgnoreConsumed()` for dx/dy. Also 96 dp threshold -> 72 dp (one natural flick reaches the column); JVM constants 288 -> 216 px; case-47 pins migrated + a new `positionChangeIgnoreConsumed()` pin.
- Gates: 47/47 / 1892, tsc, ktlint, secret-scan. Worker untouched. Release hold stands; next release versionCode > 215. Pending: owner's phone review (r75-4 hold visual + r75-5 panel + r75-6 lock fix).
- r75-6 fixes: (a) ktlint caught the dangling `positionChange` import AFTER the commit fired (my `;` chain ran gates out of order — my mistake, gates ALWAYS before commit); (b) CI 36151972921 failed on `Unresolved reference: positionChangeIgnoreConsumed` — the extension needs its own `androidx.compose.ui.input.pointer.positionChangeIgnoreConsumed` import (the sandbox has no Kotlin compiler; only CI proves compilation). Final tip `ae49b5d`, CI success (run 36152776090 above).

## r75-7 — a version you can SEE, and the column over the mic (2026-09-25)
- Owner (hold screenshot again + "tomar problem ki? send button nei voice wave nei ... once view button nei ... kichu changes na korei bar bar apk download korte bolo kno?"): the panel items are LOCKED-only; his builds never locked (r75-6 root cause). The column sat ~45 dp LEFT of the mic (Row order: pill before mic; spacer 6 + mic half 21 + pill half 18). And every dev APK said 3.9.138/215 — no visible proof of change.
- r75-7: (1) versionCode 216 / versionName 3.9.139 — every round bumps it until the release hold lifts (the r66 pin in case 36 advances with it); (2) the capsule offset gains +45 dp x so the column is CENTRED over the mic; (3) gates all green BEFORE the commit (lesson from r75-6 applied).
- Phone test protocol for the owner: download the apk from THIS green run, check the app's version shows 3.9.139, then hold-mic and swipe up — the column rises CENTRED over the mic and the panel replaces the bar. If the lock still fails, the next report must say what the release did (nothing / sent).

## r75-8 — the owner's word-for-word recorder spec (2026-09-25)
- Owner, furious ("are you joking with me? ... na parle gali debo"), spelled the WHOLE spec in words — this overrides every earlier screenshot reading: HOLD = record WITH the live wave + the lock icon above the voice button + the old slide-left dustbin animation. LOCKED (swipe up) = wave in the middle, view-once icon top-RIGHT, time top-LEFT, dustbin bottom-LEFT (click = delete/cancel), Pause in the middle, and the VOICE BUTTON ITSELF BECOMES SEND ("ar voice button send button hoye jabe") — the panel carries NO send of its own.
- r75-8: the hold bar regained LiveVoiceWave (accent, weight-1, 22 dp; bar padding 9 dp); the locked panel's bottom row is bin + wide Pause pill only; the Send moved back to the right slot as a `locked ->` branch (42 dp, accent, DoubleArrow dark glyph, fxMicAnchor + KpDoubleTapSeat, onSendVoice/onSendVoiceOnce) — the r71-19 shape returns on top of the r75-5 panel. versionCode 217 / 3.9.140 (every round bumps until the hold lifts).
- Breakage caught in-house this round: (1) case 46's brace guard caught a duplicated "} else {" pair my panel splice left (a comment-glued junk line from r75-4 was also cleaned); (2) case 32's r39-6 tail had TWO identical "locked -> Unit" pins and the r18-5 hold-comment pin needed the shorter "live HOLD strip (r75-8" literal. LESSON: pins that quote MY OWN comments break every time I reword them — pin STRUCTURE (sizes/branches), never prose; and after every splice, run the suite BEFORE prettier reflows anchors.
- Gates: 47/47 / 1892, tsc, prettier, ktlint, secret-scan. Worker untouched. Release hold stands; next release versionCode > 215. Owner phone protocol: install 3.9.140+ from the green run — hold shows wave + lock column over the mic; swipe up locks into the panel; the mic seat is Send.

## r75-9 — the approved preview port: release-decides hold, fixed column, the swallow (2026-09-26)
- The owner APPROVED Preview v5.8 ("perfect ebar implement kor prottekta details jeno thake") after eight preview rounds in the FROZEN repo (previews/voice-recorder.html). The preview workflow ended: the approved shape is now the repo's shape. His standing add-ons: view-once icon + Send = the APP'S originals ("app er original ta use korbi"), every dictated detail exactly as in the HTML.
- What shipped (preview -> repo, word for word): (1) HOLD records but NOTHING fires while the finger is down — the RELEASE decides: past the lock-at HALF (46 dp; the device-bug fix — the mid-drag 72 dp cross never landed on his 3.9.140 phone) = LOCKED, left past 88 dp = the swallow, else SEND; (2) the FIRST dominant direction owns the drag (up OR left, never both, an early y-lock can still escape low-leftward); (3) the voice button ITSELF rides the finger now (micX/micY springs) — up the column or leftward, capped; (4) the lock column is FIXED (50x172 dp, 22 dp corners both ends, bottom at the mic's bottom, +52/-65 dp) — only the button climbs it; the hand-drawn Material padlock (PadlockGlyph) hangs OPEN at rest and the shackle closes at the half (accent tint); the KeyboardArrowUp chevron sits ABOVE the middle bobbing 0 -> -7 dp in an infinite Reverse loop; (5) the cancel arm DROPS the strip's clock (offset 26 dp + fade) and raises the small dustbin (SmallDustbin, 20x22, thin hinged lid) in its place — no overlap; (6) the release-after-arm plays the SWALLOW (ComposerBinSwallow, 900 ms): the mic GLYPH alone (no ring/fill) flies seat -> open mouth, drops straight in shrinking, lid shuts; VoiceBinDrop is GONE; (7) the locked panel's bottom row is bin + wide Pause + the app's own voice-send (DoubleArrow, 0xFF10141A, KpDoubleTapSeat, honours voiceOnce / onSendVoiceOnce) BESIDE the Pause pill, and the mic seat renders NOTHING while locked ("extra kichui korbi na"); (8) locked straight-cancel (bin click) has no animation ("emni cancel hobe").
- Mechanics worth keeping: the swallow rides a ZERO-WIDTH anchor Box at the row's end (unclipped), positioned off onSizeChanged binRow + the mic's published x; swallowV is an Animatable 0->1 driving lid + flyer together. LockBadgePill offset is now FIXED (the old lockDragY ride-along is dead).
- Pins migrated: case-32 r33-11b (swallow literals), case-36 (218/3.9.141), case-47 (release-decides machine, fixed column, Send-inside-panel; DarkCard count 3->4 for the cancel-scene strip); VoiceHoldGestureTest lock 216->138 px (46 dp @3x), VoiceHoldGesture docs say release-decides now.
- Gates: 47/47 / 1892, ktlint, tsc, prettier, secret-scan — all BEFORE the commit. versionCode 218 / 3.9.141. Worker untouched. Release hold stands; next release versionCode > 215. Owner phone protocol: install 3.9.141+, hold the mic — the bar stays, the button rises, half-way the shackle closes; release up = lock, release left = the swallow, release still = send; locked panel = clock + grey wave + "1" / bin + Pause + Send beside it.

## r75-9 CI fix — the four APIs the sandbox could not prove (2026-09-26)
- CI 36178978410 failed (r75-6's lesson again, four times over): (1) THIS Compose's Animatable.animateTo block is receiver-only (`Animatable<>.() -> Unit`) — no `{ v, _ -> }`; the swallow reads `swallowT.value` (state-backed, per-frame recompose) instead of a hoisted var; (2) `Float * Dp` is a package-level extension not auto-imported — `14.dp * (1f - sink)` (Dp * Float is a member, always safe); (3) this DrawScope's drawArc args are `startAngle` / `sweepAngle` (not startAngleDegrees/sweepDegrees); (4) `Dp.roundToInt()` does not exist — `roundToPx()`. case-32's swallow pin moved with (1). Gates re-green before the fix commit.
## r76-1 — the voice recorder REBUILT from the approved preview v5.8 (2026-09-26)
- Owner (two on-device screenshots of the r75-9 build + the approved HTML): "kono bug na just eita implement korte hobe I'm tired onek arena ke bojhate bojhate but vul bhal implement hocche sara din. screenshot ta dekh ki korche ager session ar html a dekh koto sundor kore gochano" — the old recorder (Composer branches / HoldMicButton / LockBadgePill / swallow) was REMOVED and rebuilt state-by-state 1:1 from `uploads/voice-recorder.html` (preview v5.8). Only two icons come from the app: the send glyph (Icons.AutoMirrored.Filled.Send) and the view-once icon (CenteredOnceIcon).
- Shipped, state by state: IDLE pill untouched. HOLD = one 25 dp DarkCard bar (15 sp clock min-42 dp, 28 dp accent wave, 12 sp muted hint); the cancel arm SINKS the clock 26 dp (180 ms) and RAISES the small dustbin into its spot (200 ms, 100 ms late — never overlapping), wave stays accent. The seat wrap (50x46) carries the FIXED lock column 50x172 (22 dp corners, 2 dp accent border ONLY when armed, dimmed to 30% while the cancel arm holds; 22 dp padlock, shackle closes at the half; chevron a little above middle, 1.1 s bob) BEHIND the 46 dp mic (2 dp ring over the screen's own Cream fill so the column never shows through). Thresholds dp-for-px from the HTML: cancel 120 (unarm 84 hysteresis), lock-at 62, rise cap 138, slide cap 150, slop 18, low-left escape 26/44/1.8; NOTHING fires mid-drag, the RELEASE decides (an armed cancel stays armed down to the unarm line). LOCKED = 25 dp card: grey 28 dp wave + 40 dp once circle (1.5 dp accent ring armed), then 48 dp red bin (DIRECT cancel) + 48 dp Pause pill + 48 dp accent Send with the app's glyph in 0xFF0D1524, right beside Pause; the seat renders nothing. Swallow retimed to the HTML: lid open by ~60 ms, glyph flies 300 ms, drops 200 ms shrinking+fading, lid shuts ~59-71% of the 900 ms.
- Gone with the rebuild: the DoubleArrow chevron send (and its import), the old 42 dp ring-only mic, the +52/-65 dp column offset hack. DarkCard count in the composer slice is now 3.
- Pins migrated in the same commit: case-47 section 19 (machine/panel/column/palette checks rewritten for the new literals, r72 seat pin loosened to a count), case-32 (hold-bar comment, hint 12.5→12 sp, grey wave 26→28 dp, mic ring 1.5→2 dp + Cream fill, `.size(46.dp)` count === 1), case-36 version pin, VoiceHoldGestureTest distances 264/138 → 360/186 px.
- versionCode 219 / versionName 3.9.142 (every-round bump until the release hold lifts).
- Gates BEFORE the commit: 47/47 (1891 assertions), tsc, prettier, ktlint, secret-scan, validate-android. Worker untouched → no deploy. Release hold stands; next release versionCode > 215. Owner phone protocol: install 3.9.142 from the green run — every state must read like the HTML: hold bar in the pill's slot, column centred behind the mic, locked panel with Send beside Pause.
- Lesson (third time): a splice that replaces up to a "} else {" / "Spacer(" marker silently drops the branch's closing brace — the case-46 brace guard caught it before push; always end the replacement with the branch's own "}".
- CI on the rebuild: 1st red = RecorderLockedPanel's combinedClickable needed its own @OptIn(ExperimentalFoundationApi) (follow-up 9d672f1); 2nd red = the JVM left-slide test's -300 px no longer crossed the new 360 px cancel (follow-up 2b535b5, dx -400); run 36188248105 success on 2b535b5 — that green run's kuchupuchu-apk artifact is the 3.9.142 phone build.
## r76-2 — on-device fixes from the owner's first phone verdict on the rebuild (2026-09-26)
- Owner: "swipe left kora jai na swipe up kora jai na tap korei slide left korle hoi but dustbin er cap ta onek boro mic icon taw dustbin er vetore jai na. swipe up er jonno je bar ta thakbar kotha otaw nai."
- Root causes + fixes (commit 06e38df): (1) the axis read ONE event's delta against the 18 dp slop — slow deliberate up-swipes never crossed it on the phone (fast left flicks did) → axis now decides on the accumulated pull (totX/totY); (2) the column's 120 ms fade was published through SideEffect whose lambda reads state UNTRACKED — the alpha never advanced on-device, the column sat invisible → LaunchedEffect keyed on (lockAlpha, lockArmed, cancelArmed, micX, micY) publishes per frame; (3) the swallow flyer aimed 8 dp left of the bin centre and stopped shallow → mouthX 38 dp from the row's left, drop lands at the bin centre; (4) the lid was a 22 dp plank → thin 14 dp cap + 4 dp handle.
- Lessons: (a) gesture slop must be tested against ACCUMULATED distance, never a single move event — event batching on real devices makes per-event thresholds a lottery; (b) never publish state through SideEffect when the value comes from an animation — its lambda is outside snapshot tracking, so animations never advance; (c) his "swipe left kora jai na" was the DESIGN (press first) — the HTML behaves the same; what was broken was up only.
- CI 36192101429 success on 06e38df — that run's kuchupuchu-apk is the new phone build (3.9.142 + r76-2). Release hold stands.
## r76-3 — the column shows at once, like the HTML; the build says its name (2026-09-26)
- Owner (furious, and right): "amar html a to shob system deoa ache lock icon kothai thakbe locked thakle kemon hobe taw keno vul hocche bar bar? ... html ta full read na korei implement korcho? ekdom shortcut use korla?" — the port had invented an alpha gate the reference never asked for (the HTML's toHold() un-hides the column instantly). r76-3 removes the fade/gate: LockColumn composes whenever recording && !locked, dimmed 30% only while the cancel arm holds; the publish stays a tracked LaunchedEffect. versionCode 220 / 3.9.143 — the phone build says its own name so verdicts are unambiguous (r75-7 precedent).
- CI 36195133214 success on 98b84e9. Worker untouched. Release hold stands.
- LESSON (write it on the wall): port the reference STRUCTURE, not an interpretation of it — every added gate/shortcut is a bug the owner will hold in his hand. When a round's phone verdict says "X nei", first diff the code against the reference line by line for X, before inventing a device theory.

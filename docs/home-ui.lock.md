# Home UI lock

The Home screen is locked.

Do not change markup or CSS in:

- `src/web/pages/home.tsx`
- `src/web/pages/home.css`

and do not restyle Home rules in `src/web/styles.css` (`.feed-page`, `.mind-bar`, `.story-*`, `.post-card`, `.people-block`).

Unlock only if the user says so in this conversation.

<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- Owner round 16: in-app updates — installing the downloaded APK
         from inside the app needs this. -->
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />
    <!-- Phone auth (OTP-less): SubscriptionManager.activeSubscriptionInfoList is
         gated on READ_PHONE_STATE at runtime. A denial is not fatal — the
         login flow reports PERMISSION_DENIED and the worker's grace path
         (DEVICE_ONLY + old-device approval) still protects the account. -->
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <!-- Owner round 28: phone-book match (All contacts / search). Asked in-context, never at launch. -->
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />
    <!-- Every Bluetooth audio call (setCommunicationDevice on a headset, opening
         SCO) throws SecurityException from API 31 without BLUETOOTH_CONNECT; the
         routing code's runCatching swallowed that, so connected buds were never
         used and the call stayed on the phone. maxSdkVersion keeps the legacy
         permission for startBluetoothSco on API <= 30. -->
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <uses-permission android:name="android.permission.BLUETOOTH" android:maxSdkVersion="30" />
    <uses-feature android:name="android.hardware.bluetooth" android:required="false" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <!-- Let the user whitelist the app from battery savers so FCM pushes and
         calls arrive instantly (many OEMs delay/kil background network). -->
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <!-- Attach-sheet recent grid (photos/videos on this device) -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />
    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT" />
    <!-- Call foreground service types (call round re-enables the service itself) -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MICROPHONE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_CAMERA" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PROJECTION" />
    <!-- §31 self-managed calling: lets this app report and control ITS OWN VoIP calls
         through android.telecom (a headset's call button, a watch, the platform's
         one-call-at-a-time rules for emergency and GSM calls) without becoming the
         device's dialer. Normal protection level: granted at install time, no runtime
         prompt, and it grants no access to anything but this app's own calls. -->
    <uses-permission android:name="android.permission.MANAGE_OWN_CALLS" />
    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <uses-feature android:name="android.hardware.microphone" android:required="false" />
    <!-- Android 11+ package visibility: without this the system hides every
         viewer app, so ACTION_VIEW for documents threw and "Open" did nothing. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:mimeType="*/*" />
        </intent>
    </queries>

    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="KuchuPuchu"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:largeHeap="true"
        android:hardwareAccelerated="true"
        android:theme="@style/Theme.KuchuPuchu">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:showWhenLocked="true"
            android:turnScreenOn="true"
            android:configChanges="orientation|screenSize|keyboardHidden"
            android:launchMode="singleTop"
            android:windowSoftInputMode="adjustResize"
            android:theme="@style/Theme.KuchuPuchu">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!-- Push delivery: high-priority FCM data messages wake the app
             just-in-time. There is deliberately NO other service here. A
             remoteMessaging keep-alive foreground service (PR #7, and later
             armed from onPause) was tried and removed: the only thing it
             reliably produced was an ongoing "KuchuPuchu is listening"
             notification, it did not make the OEM let a backgrounded process
             receive anything it was not already going to deliver, and it died to
             a recents swipe anyway. Re-introducing it needs evidence that the
             process IS being reached (see KpDiag / Settings -> Push log). -->
        <!-- Owner round 32 (items 1A/1B/26): the action MUST be
             com.google.firebase.MESSAGING_EVENT. The FCM SDK resolves the
             app's FirebaseMessagingService subclass by that exact action
             (ServiceStarter / WithinAppServiceConnection bind to it; the SDK's
             own base service is registered on it at priority -500). This
             filter said "com.google.firebase.messaging.RECEIVE" since v2.1.1 —
             a string that exists nowhere in the SDK — so every push resolved
             to the SDK's BASE service, whose onMessageReceived is an empty
             method: the app's own Reply / Like / Mark-as-read card, the
             missed-call card with Call back / Message, the fullscreen ring
             and the ring-card retraction never ran from a push. The only
             cards the owner ever saw from a push were the OS-drawn payload
             cards (generic glyph, no buttons); the rich cards he saw while
             the app was "recently active" came from the WebSocket path. -->
        <service
            android:name=".KpPushService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>
        <!-- Defaults for the OS-drawn payload card (a genuinely dead process):
             the brand status icon + colour instead of the generic glyph, and
             the message channel when the push names none. -->
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_icon"
            android:resource="@mipmap/ic_stat_kp" />
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_color"
            android:resource="@color/colorPrimary" />
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_channel_id"
            android:value="kp_messages_v2" />
        <!-- Notification delegation stays ON (FCM default): a payload push is
             only sent when no live socket exists, and a card Play services
             draws without starting the app is the only card a process the
             OEM refuses to start can ever show. Stale ring cards are handled
             by the shared FCM tag (missed-call card replaces the ring card)
             and tag-aware cancels in KpNotify, not by disabling delegation. -->
        <!-- Shares picked-camera photos and downloaded files with system apps. -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
        <!-- Call foreground service + notification action receivers -->
        <service
            android:name=".CallService"
            android:exported="false"
            android:foregroundServiceType="microphone|camera|mediaProjection" />
        <!-- §31: what android.telecom binds to mirror the live call (KpTelecom /
             KpCallConnection). exported=true is unavoidable — the caller is the system —
             and BIND_TELECOM_CONNECTION_SERVICE is what keeps it to the platform's Telecom
             process alone. The app's own call UI stays authoritative: a self-managed
             account is explicitly the mode where the platform shows no in-call UI. -->
        <service
            android:name=".KpConnectionService"
            android:exported="true"
            android:permission="android.permission.BIND_TELECOM_CONNECTION_SERVICE">
            <intent-filter>
                <action android:name="android.telecom.ConnectionService" />
            </intent-filter>
        </service>
                <!-- Owner round 16: in-app update installer result. -->
        <receiver
            android:name=".KpUpdateReceiver"
            android:exported="false" />

<receiver
            android:name=".KpNotifActionReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="app.kuchupuchu.android.NOTIF_REPLY" />
                <action android:name="app.kuchupuchu.android.NOTIF_LIKE" />
                <action android:name="app.kuchupuchu.android.NOTIF_MARK_READ" />
            </intent-filter>
        </receiver>

        <!-- Push registration refresh after an update (force-stopped app) or a
             reboot; see KpWakeupReceiver. -->
        <receiver
            android:name=".KpWakeupReceiver"
            android:enabled="true"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
            </intent-filter>
        </receiver>

        <receiver
            android:name=".CallActionReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="app.kuchupuchu.android.CALL_DECLINE" />
            </intent-filter>
        </receiver>
        <receiver
            android:name=".ReminderReceiver"
            android:exported="false" />
    </application>
</manifest>
